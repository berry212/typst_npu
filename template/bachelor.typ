#import "/template.typ": (
  algorithm, algorithm-ref, capfig, capsubfig, captab, equation-note, indent, multicite, no-number, nwpu-thesis,
)

#show: nwpu-thesis.with(
  anonymous: false, // 是否开启盲审模式
  title: "基于数据集蒸馏的高效医学影像分类",
  author: "陈浩明",
  major: "数据科学与大数据技术",
  supervisor: ("夏勇", "教授"),
  submit-date: (year: 2026, month: 6),
  abstract: [
    医学影像分类在疾病筛查与辅助诊断中具有重要价值，但高质量标注数据稀缺、隐私限制与训练成本高等问题限制了模型推广。针对上述瓶颈，本文提出一种面向医学图像的高效数据集蒸馏方法，通过在极少样本上训练即可获得接近全量数据的分类性能。方法以预训练潜在扩散模型为基础，采用低秩适应 (Low-Rank Adaptation, LoRA) 进行轻量域适配，使生成模型更好刻画医学图像纹理与结构；在潜在空间对全类别特征进行全局聚类，避免按类别聚类带来的信息割裂与类别不平衡影响；以加噪聚类中心为起点执行反向去噪生成蒸馏图像，由教师模型生成软标签提供给下游模型进行训练，提升了训练效率与分类性能。

    在 DermaMNIST、BloodMNIST 与 APTOS-2019 三个公开医学图像数据集上的实验表明，本文方法在多种每类图像数（Image Per Class, IPC）设置下均优于随机选择与 K-Means 近邻等基线，并在较高 IPC 时接近全量训练性能；消融实验验证了全局聚类与软标签训练的有效性；定性结果显示蒸馏图像具有较好真实感与类内多样性，同时显著降低训练时间。上述结果表明，所提出方法在保证精度的同时实现了数据与计算的高效压缩。
  ],
  keywords: ("医学影像分类", "数据集蒸馏", "高效"),
  abstract-en: [
    Medical image classification is crucial for disease screening and assisted diagnosis, yet it is hindered by scarce high-quality annotations, strict privacy constraints, and high training costs. To address these challenges, this thesis proposes an efficient dataset distillation method tailored to medical images, enabling models to achieve near full-data performance with only a small synthetic set. We start from a pretrained latent diffusion model and apply LoRA-based domain adaptation to better capture medical textures and structures. In the latent space, we perform global MiniBatchKMeans clustering across all classes and assign prototype labels by majority voting, which mitigates information fragmentation and class-imbalance effects induced by per-class clustering. Prototypes are then refined into distilled images via reverse denoising, and a teacher-model soft-label scheme with precomputed batches is introduced to improve training efficiency and knowledge transfer.

    Experiments on DermaMNIST, BloodMNIST, and APTOS-2019 demonstrate that the proposed method consistently outperforms random selection and K-Means neighbor baselines across multiple IPC settings, and approaches full-data performance at higher IPC values. Ablation studies verify the contributions of global clustering and soft-label training, while qualitative results show realistic and diverse distilled images with significantly reduced training time. These findings indicate that the proposed approach achieves effective compression of data and computation without sacrificing accuracy.
  ],
  keywords-en: ("Medical image classification", "Dataset distillation", "Efficiency"),
  //   appendix: [
  //     == Test
  //     附录内容……
  //     #capfig(
  //       image("figures/example.jpg", width: 45%),
  //       caption: [图片测试],
  //       label: <test1>,
  //     )
  //   ],
  acknowledgement: [
    时光荏苒，四年的大学生活即将画上句号。在本文完成之际，谨向所有在求学路上给予我帮助和支持的人致以诚挚的谢意。

    衷心感谢夏勇教授，以及叶艺文师兄。从论文选题、方案设计到实验推进和文稿撰写，始终给予我耐心的指导和宝贵的建议。

    感谢实验室的各位同学在讨论中提供的思路和启发，与你们的交流让我开阔了视野。感谢室友们在生活中的陪伴和包容，让大学时光更加充实和温暖。

    感谢我的一位中学同学，好朋友李文轩，在我算力紧张的时候免费提供了一台 5090 显卡的机器，助我完成许多实验。以及感谢这求学路上所有支持我和给我提供帮助的朋友。

    特别感谢我的家人。你们的无私奉献和默默支持是我能够安心学业、不断进步的坚强后盾。感谢所有关心和帮助过我的人。
  ],
  design_summary: [
    本毕业设计围绕"基于数据集蒸馏的高效医学影像分类"这一课题展开。针对医学图像标注稀缺、隐私限制和训练成本高等问题，提出了一种结合LoRA微调扩散模型与潜在空间全局聚类的数据集蒸馏方法。

    在数据生成环节，利用LoRA对预训练潜在扩散模型进行域适配，使其能够更好地刻画医学图像的纹理与结构特征。在信息压缩环节，通过VAE编码器将原始图像映射到潜在空间后，对所有类别的潜在特征进行全局MiniBatchKMeans聚类，以聚类中心作为原型，再经反向去噪过程生成蒸馏图像。在模型训练环节，采用教师模型为蒸馏图像生成软标签，指导学生模型训练。

    实验结果表明，本文方法在DermaMNIST、BloodMNIST和APTOS-2019三个医学图像数据集上均取得了有竞争力的分类性能。在IPC=200时，三个数据集上的准确率分别达到81.64%、98.27%和80.54%，接近全量数据训练的水平。消融实验验证了全局聚类和软标签训练各自的有效性。与D⁴M、MGD³、DDQO等现有方法相比，本文方法在收敛速度和最终性能上均表现出优势。

    通过本次毕业设计，我深入学习了数据集蒸馏、扩散模型、参数高效微调等前沿技术，提升了独立科研和工程实践能力。同时也认识到，当前方法在大规模高分辨率医学影像的可扩展性、多模态蒸馏拓展等方面仍有改进空间，未来可在此基础上做进一步探索。
  ],
)

= 绪论

== 课题研究背景及意义

医学影像分类技术已广泛用于各类医疗环节。在肺部CT图像分析中，自动分类能帮助医生更快识别病变，例如肺炎。同样，在眼科筛查中，对于糖尿病视网膜病变这类疾病，基于深度学习的图像分类技术也提升了筛查效率。

深度学习的发展，让高性能影像分类模型对训练数据提出了更高要求，既需要数据量大，也需要质量好。但医学图像本身比较特殊，带来了一些困难。构建高质量标注的医学图像数据集成本较高，需要专业医生花较长时间标注病灶。同时，由于患者隐私保护的规定，不同机构之间很难共享数据，导致数据缺乏的问题越来越突出。此外，原始医学图像的数据量很大，存储成本高，训练模型的计算开销也大。这使得资源有限的中小医院难以部署先进的AI诊断系统。

数据集蒸馏技术可以解决上述问题。与知识蒸馏不同，它不压缩模型，而是把大数据集压缩成一个小型的合成数据集。这个合成数据集保留了原始数据的核心特征和知识，可以用来替代原数据集进行训练。在保留疾病诊断关键特征（如病灶形态、纹理等）的前提下，该方法能明显降低存储需求和计算开销，让资源有限的医疗机构也能部署先进分类模型。同时，合成数据集不使用真实的患者数据，可以避免泄露患者隐私，从而打破机构之间共享数据的障碍。

== 医学影像分类技术发展

医学影像分类的研究方式，从传统的手工设计特征，如今已慢慢转向了深度学习自动学习特征。在深度学习技术普及之前，医学影像分类主要靠人工设计的特征提取器，再结合经典的机器学习方法。一个典型的流程是：先用一些算法从图像里提取局部纹理和边缘特征，比如尺度不变特征变换、方向梯度直方图或局部二值模式，然后用支持向量机或随机森林这类分类器做出判断。这种方法的优点是容易解释，并且不需要太多数据。它在早期的皮肤镜图像分析、乳腺X光微钙化检测等任务上取得了一定效果。不过，手工特征严重依赖专家经验，很难完整表达医学图像中复杂多变的病灶形态和纹理。而且，在不同设备、不同采集方式下，它的泛化能力也比较差。

卷积神经网络（Convolutional Neural Networks, CNN）在 ImageNet 大规模视觉识别竞赛中取得了突破性进展，之后医学影像分类很快进入了深度学习时代。深度卷积网络可以从原始像素中自动学习视觉特征，不需要人工设计特征，这大大提升了模型对复杂医学模式的建模能力和泛化能力。以ResNet为代表的残差网络，通过引入跳跃连接解决了深层网络难以训练的问题，使得训练几十层甚至上百层的网络变得可行。在皮肤病变分类、糖尿病视网膜病变检测、胸部X光多病种识别等任务上，深度学习方法的效果已经达到甚至超过了经验丰富的放射科医生。

近年来，Vision Transformer（ViT）这类基于自注意力机制的架构，开始从自然图像领域被用到医学图像领域。与卷积网络依赖局部感受野逐步整合上下文不同，ViT把图像分成固定大小的图像块，把每个图像块看作一个“词”，然后利用多头自注意力机制，在全图范围内建模任意两个位置之间的关系。这种特性对医学图像特别有意义。病灶与周围组织的空间关系，对称器官之间的对比信息，这些全局上下文都可以直接被捕获，不需要靠堆叠很多卷积层。在病理全切片图像分析、三维CT体积分类等需要远距离空间推理的任务上，Transformer架构已经展示出优于传统卷积网络的潜力。

医学图像数据集和自然图像不一样，面临一系列独特的挑战。类别不平衡是一个问题。在临床中，罕见病的阳性样本通常远少于正常样本，这种严重的数据分布偏移会让模型倾向于预测多数类，对少数类的敏感度非常低。细粒度差异是另一个困难。不同疾病之间可能只有微小的纹理变化或边界形态差异，例如不同级别的糖尿病视网膜病变，区别只是微血管瘤数量和出血点分布的不同，这对模型的判别能力要求很高。标注稀缺问题更加突出。高质量的医学标注需要经过专业训练的放射科或病理科医生逐个勾画，耗时很长且成本很高，导致大规模标注数据集非常少见。此外，不同医院之间因为设备型号、成像参数、患者人群等差异，会产生域偏移问题，使得模型换到另一家医院使用时性能明显下降。

为了解决上述挑战，研究者提出了多种数据高效学习方法。少样本学习通过度量学习或元学习的方式，让模型在只有少量标注样本的新类别上快速适应。在皮肤镜图像分类和胸部X光罕见病识别中，基于原型网络和匹配网络的少样本方法已经取得初步效果。自监督学习利用无标注数据内部的结构来设计前置任务，比如对比学习中的实例判别、掩码自编码器中的图像重建，用这些任务预训练模型，从而大大减少对人工标注的依赖。在医学图像领域，先用大量无标注影像（如胸部X光或病理切片）预训练的自监督模型，再微调到下游分类任务时，效果明显优于随机初始化甚至ImageNet预训练模型。数据增强则是通过对已有样本做一些保留语义的变换（如旋转、裁剪、颜色变化），或者用生成模型合成新样本（如利用生成对抗网络或扩散模型生成逼真的病灶图像），来增加训练数据的多样性和数量。

上述方法在缓解标注数据和计算资源不足的问题上各有作用，但它们通常无法从根本上改变依赖大量数据的局面。少样本学习仍然需要充足的基础类别训练数据。自监督学习需要海量无标注数据来进行预训练。数据增强带来的信息增益是有上限的。而数据集蒸馏是一种以数据压缩为核心的新方法，它把原始的大规模训练集浓缩成一组信息密度高的合成样本，从而有效降低存储、传输和训练过程中的开销。

== 数据集蒸馏领域研究现状

数据集蒸馏的核心目标，是把大规模数据集压缩成一个小的合成数据集。模型在这个合成集上训练后，效果能接近在原始数据上训练的结果@wang2018dataset，如@DD-framework 所示。它的基本优化问题可以写成：找一个合成数据集 S，在它上面多步训练后，让模型在原始数据集 T 上的损失期望尽量小。早期工作使用时间反向传播来实现这个目标，但存在梯度变化大、计算效率低等问题。后来，研究者从优化算法入手，提出了随机截断时间反向传播@feng2024embarrassingly 和基于泰勒近似的 Teddy 框架@yu2024teddy，来降低计算复杂度。

#capfig(
  image("figures/DD.png", width: 100%),
  caption: "数据集蒸馏整体框架示意图",
  label: <DD-framework>,
)

目前，数据集蒸馏方法主要围绕几种匹配思路展开。轨迹匹配方法把视角从单步梯度扩展到整个训练轨迹，通过对齐参数优化路径来获得更稳定的蒸馏效果#multicite("zhao2021dataset", "cazenavette2022dataset")。后续研究从轨迹稳定性和大规模可扩展性等方面做了改进。例如 TESLA 方法通过分解梯度计算，在内存不变的情况下实现了对 ImageNet-1K 的蒸馏。分布匹配范式放弃了计算成本高的双层优化，直接在特征空间中对齐合成数据与原始数据的分布@zhao2023dataset。后续工作分别从特征精炼@zhang2024dance、注意力对齐@sajedi2023dataDAM 和高阶矩匹配@zhang2024m3d 等方面进行了深化。

#capfig(
  image("figures/match_method.png", width: 100%),
  caption: "基于匹配的方法示意图",
  label: <DD-match>,
)

在处理大规模高分辨率场景时，近年的关键突破是解耦优化和引入生成式方法。SRe2L 框架把蒸馏过程分成“压缩、恢复、重标记”三个阶段，其中恢复阶段通过匹配批量归一化统计量来生成数据，并结合软标签显著提升了性能@yin2023sre2l。之后，SC-DD 用自监督预训练来增强统计量@zhou2024selfsupervised，G-VBSM 引入了多骨干网络一致性匹配@shao2024generalized，EDC 融合了真实图像初始化和平坦性正则化@shao2024elucidating，这些工作共同推动了大规模蒸馏的发展。同时，软标签因为包含丰富的语义信息，也成了提升性能的关键模块。另一个重要方向是基于生成模型的蒸馏。IT-GAN 利用冻结的 GAN 优化潜向量，生成信息丰富的样本。扩散模型由于生成质量高，衍生出了 D⁴M@su2024d4m 和 D³M@abbasi2024d3m 等工作，实现了在隐空间中的高效压缩和跨架构泛化。此外，选择性蒸馏、多样性增强和无损蒸馏等技术，也从不同方面提升了合成数据的代表性和训练效率。

在架构泛化性、鲁棒性和非独立同分布适应性方面，研究也在持续推进。跨架构泛化通过梯度平衡、特征解耦以及模型池集成等策略，提升了合成数据集在不同模型之间的迁移能力。鲁棒性方面，GUARD 等方法通过曲率正则化等手段增强了蒸馏数据集的对抗鲁棒性。针对非独立同分布数据，研究者还提出了 OOD 检测蒸馏和去偏与公平性蒸馏等方案。在联邦学习等非中心化场景中，则通过传输合成数据来降低通信开销并保护隐私。

然而，上述数据集蒸馏方法大多是在通用自然图像场景下设计和验证的。当直接用到医学图像领域时，其有效性会面临一些尚未充分研究的问题。现有的生成式蒸馏方法普遍直接采用在大规模自然图像上预训练的扩散模型，没有考虑自然图像与医学图像之间的明显差异。X 光、CT、病理切片等影像有各自特定的灰度分布、纹理模式和解剖结构。通用编码器提取的特征很难准确描述这些医学特有的视觉属性，这就限制了后续聚类和生成的质量上限。基于聚类的蒸馏方法（如 D⁴M、MGD³、DDQO）都采用逐类独立聚类的策略。但医学图像中，不同疾病类别在特征空间里常常有交叉和重叠，例如不同等级的视网膜病变之间构成连续渐变的谱系。逐类聚类忽略了跨类别的潜在特征共享关系，在有限的合成样本预算下会损失宝贵的判别信息。另外，在软标签训练的工程实现方面，现有方法通常每轮训练都要对合成样本实时做数据增广和教师模型前向传播。在追求高压缩比和训练效率兼顾的场景下，这种做法还有优化的空间。

针对上述问题，本文提出了一种面向医学图像的高效数据集蒸馏方法。整体方法在保留扩散模型高质量图像生成能力的基础上，针对医学场景的实际需求做了系统优化，力求用较低的压缩代价实现高效的医学影像分类。

== 论文主要工作

本文针对医学影像分类任务，提出一种数据集蒸馏方法。在蒸馏之前用 LoRA 技术对预训练扩散模型进行轻量的域适配，让它更好地理解医学图像的特征，在蒸馏阶段使用全局聚类，打破逐类独立聚类的限制，让不同类别中相似的特征也能分配到同一个聚类中心。在模型训练环节，先在原始数据集上训练一个教师模型，为蒸馏图像生成软标签作为监督信号，下游的分类模型使用蒸馏图像和软标签进行监督训练。整体框架包含三个阶段：扩散模型的域适配与微调、潜在空间中的全局聚类与蒸馏生成、基于软标签的模型训练。各阶段的具体工作如下。

=== 扩散模型域适配

为了让通用扩散模型更好地适应医学图像，本文在目标医学图像数据集上对预训练的潜在扩散模型进行参数高效的微调。采用 LoRA 技术@hu2022lora，只在扩散模型 U‑Net 的注意力层中插入少量可训练的低秩矩阵，同时冻结原始权重。这样做既保留了预训练模型在自然图像上积累的丰富生成经验，又用很小的计算代价让模型学会医学图像的特有风格。经过 LoRA 微调后，扩散模型对医学图像的理解更加准确，为后续的聚类和蒸馏图像合成打下了良好基础。

=== 潜在空间全局聚类与蒸馏生成

在蒸馏阶段，本文放弃了直接在高维像素空间进行优化的传统做法，转而利用扩散模型的潜在空间来完成信息压缩与合成。先用冻结的VAE编码器把所有原始训练图像映射到低维潜在空间，得到隐变量集合。然后，对所有类别的隐变量做全局MiniBatchKMeans聚类，提取聚类中心。与现有方法中常见的逐类独立聚类不同，全局聚类策略允许一个聚类中心聚合来自不同类别的相似潜在特征，从而捕捉跨类别的特征共享关系，在合成样本数量极其有限的情况下，尽可能多地保留判别信息。

蒸馏图像的生成以聚类中心为基础。将聚类中心作为去噪过程的起点，加入适量噪声后，用文本提示词引导扩散模型逐步去噪，最后还原成像素空间的合成图像。整个过程在低维潜在空间中进行，计算负担大大减轻。同时依靠扩散模型强大的图像生成能力，合成图像既保持了医学图像的真实视觉语义特征，又不会在训练时明显降低模型性能。

=== 基于软标签策略的模型训练

在模型训练阶段，本文采用一种基于软标签的高效训练策略。先在原始完整数据集上训练一个分类性能较好的教师模型（ResNet-18），然后用这个教师模型对蒸馏图像进行推理，得到经过温度平滑的类别概率分布作为软标签。与传统的独热硬标签相比，软标签不仅指出了图像属于哪一类，还携带了不同类别之间的相似程度信息。在训练样本极少的情况下，这为学生模型提供了更丰富的学习指引。学生模型在蒸馏数据量很小、训练轮次大幅减少的条件下，能够充分吸收教师软标签中的知识，在多个医学图像数据集上取得了接近全量数据训练的分类精度。

== 论文结构安排

本论文围绕基于数据集蒸馏的高效医学影像分类展开。全文共分五章，各章内容安排如下。

第一章为绪论。阐述在医学图像高效训练背景下，数据集蒸馏的研究意义与应用需求。系统梳理国内外数据集蒸馏技术的研究现状，重点分析现有方法在医学场景中的不足，由此引出本文的主要工作，并给出整体结构安排。

第二章介绍本文涉及的基础理论与关键技术。回顾数据集蒸馏的基本概念与主要范式，包括梯度匹配、轨迹匹配和分布匹配等方法。随后详细描述扩散模型的工作原理，特别是潜在扩散模型的框架，以及参数高效微调技术LoRA。同时介绍聚类算法和数据集蒸馏中的软标签策略，为后续方法章节提供理论支撑。

第三章详细阐述本文提出的医学图像蒸馏方法。整体框架分为三个核心模块。第一，利用LoRA对通用扩散模型进行面向目标医学数据集的微调，获得域适应的生成能力。第二，在潜在空间中对全部类别的图像特征执行全局聚类，获得具有代表性的聚类中心，根据聚类中心通过扩散模型反向去噪生成蒸馏图像。第三，在训练阶段采用教师模型生成软标签，用于指导学生模型学习。这一章将逐一说明各模块的设计动机、数学表达与实现细节。

第四章为实验验证与分析。介绍所使用的医学图像数据集、实验设置与具体实现细节。展示本文方法在不同压缩比下的分类性能，通过消融实验说明各模块的贡献，并结合 t-SNE 可视化进行分析。接着与其他数据集蒸馏方法做进一步对比，展示本文方法的有效性。最后是定性分析，可视化本文合成的蒸馏图像，并对比基于匹配的方法生成的蒸馏图像，最后还展示了蒸馏数据集训练相比全量数据集训练的速度提升。

第五章对全文工作进行总结，指出当前方法的局限性，展望未来在更大规模医学数据集和多模态蒸馏等方面的潜在研究方向。

= 相关基础理论与技术

== 数据集蒸馏基础

数据集蒸馏的目标是把大规模原始数据集压缩成一个信息高度集中的合成数据集。设原始数据集为 $cal(T) = {(upright(bold(x))_i , y_i)}_(i = 1)^(| cal(T) |)$ ，合成数据集为 $cal(S) = {(upright(bold(s))_j , tilde(y)_j)}_(j = 1)^(| cal(S) |)$，且 $| cal(S) | << | cal(T) |$，要求在 $cal(S)$ 上训练的模型性能，尽量接近在完整 $cal(T)$ 上训练的结果。这个想法在 2018 年由 Wang 等人首次提出@wang2018dataset。该问题可以写成下面的形式

$
  cal(S)^* = arg min_cal(S) med EE_(theta^((0)) ~ Theta) med ell (cal(T); theta_cal(S)^((T))),
$
$
  theta_cal(S)^((t)) = theta_cal(S)^((t - 1)) - alpha nabla_(theta_cal(S)^((t - 1))) ell (cal(S); theta_cal(S)^((t - 1))),
$

#equation-note[
  其中 $Theta$ 是参数初始化的分布，$T$ 为训练步数，$alpha$ 为学习率。简单说，就是用合成数据集训练一个网络，再用原始数据集和这个网络计算损失，然后反向传播更新合成数据集。这是一个双层优化问题，因此这种方法计算效率很低。后续研究大多围绕如何高效、稳定地近似求解这个问题做了很多探索。
]

=== 经典迭代范式

早期工作使用通过时间的反向传播（Back-Propagation Through Time, BPTT）直接计算梯度，但存在梯度变化大、长期依赖不好捕捉等问题@wang2018dataset。Feng 等人提出了随机截断 BPTT，通过截断和随机化策略来稳定梯度并提升效率@feng2024embarrassingly。Yu 等人进一步提出了 Teddy 框架，利用一阶泰勒展开把双层优化简化成计算更友好的形式

$
  ell (cal(T); theta_cal(S)^((T)) ) approx ell (cal(T); theta_cal(S)^((T - 1)) ) - alpha thin g_cal(T)^((T - 1)) dot.op g_cal(S)^((T - 1)) ,
$

#equation-note(prefix: none)[
  其中 $g_cal(T)^((T - 1)) = nabla_theta ell (cal(T); theta_cal(S)^((T - 1)))$ 为在原始数据集上计算的梯度，$g_cal(S)^((T - 1)) = nabla_theta ell (cal(S); theta_cal(S)^((T - 1)))$ 为在合成数据集上计算的梯度。这样就解除了对多步计算图的存储和反向传播依赖@yu2024teddy。这些优化为后来的匹配式方法打下了基础。
]

=== 匹配式蒸馏方法

为了避开 BPTT 的高成本，研究者提出了一系列匹配式方法。它们的核心思想是让合成数据在模型训练过程中，复现出与原始数据相似的某种统计信号。

*梯度匹配* (Gradient Matching) 直接对齐模型在合成数据和真实数据上计算出的梯度。其典型目标为

$
  min_cal(S) EE_(theta_0) [sum_(t = 0)^(T - 1) D(nabla_theta ell^cal(S) (theta_t), nabla_theta ell^cal(T) (theta_t))],
$

#equation-note[
  其中 $D(\cdot,\cdot)$ 常用余弦相似度或欧氏距离@zhao2021dataset。梯度匹配对单步优化的特性比较敏感，不容易捕捉到长期的训练动态。
]

#algorithm(
  title: [梯度匹配蒸馏],
  input: [原始数据集 $cal(T)$，每类图像数 IPC，总训练步数 $T$，学习率 $alpha$],
  output: [合成数据集 $cal(S)$],
  no-number[*随机初始化合成数据集 $cal(S) = {(upright(bold(s))_j, tilde(y)_j)}$*],
  no-number[*for* $t = 0, 1, dots, T-1$ *do*],
  indent[随机采样参数 $theta_t$],
  indent[计算合成数据梯度 $g_cal(S) = nabla_theta thin ell (cal(S), theta_t)$],
  indent[计算真实数据梯度 $g_cal(T) = nabla_theta thin ell (cal(T), theta_t)$],
  indent[计算梯度匹配损失 $cal(L) = D(g_cal(S), g_cal(T))$],
  indent[更新合成数据 $cal(S) arrow.l cal(S) - alpha nabla_cal(S) cal(L)$],
  no-number[*end for*],
  no-number[*return* $cal(S)$],
)

*轨迹匹配* (Trajectory Matching) 把匹配对象从单步梯度扩展到了整个参数轨迹。Cazenavette 等人提出的匹配训练轨迹（MTT）方法，会预先记录在真实数据上训练得到的“专家轨迹”，然后优化合成数据，使得在合成数据上训练出来的参数序列尽量贴近专家轨迹@cazenavette2022dataset。该方法能建模长期依赖，因此稳定性明显提升，但计算开销也较大。为此，FTD通过正则化鼓励轨迹变得平直，从而减小累积误差。TESLA 则将梯度计算分解为序列批次，可以在内存不变的情况下处理 ImageNet‑1K 等大规模数据。

#algorithm(
  title: [轨迹匹配蒸馏],
  input: [原始数据集 $cal(T)$，每类图像数 IPC，专家轨迹步数 $M$，训练步数 $T$],
  output: [合成数据集 $cal(S)$],
  no-number[*在 $cal(T)$ 上训练并储存专家轨迹 ${theta_t^**}$*],
  no-number[*随机初始化合成数据集 $cal(S)$*],
  no-number[*for* $m = 0, 1, dots, M-1$ *do*],
  indent[随机采样专家步 $t tilde cal(U)({0, dots, T})$],
  indent[在 $cal(S)$ 上训练 $N$ 步得到学生参数 $hat(theta)_(t+N)$],
  indent[计算轨迹匹配损失 $cal(L) = \|hat(theta)_(t+N) - theta_(t+M)^** \|_2^2$],
  indent[更新合成数据 $cal(S) arrow.l cal(S) - alpha nabla_cal(S) cal(L)$],
  no-number[*end for*],
  no-number[*return* $cal(S)$],
)

*分布匹配* (Distribution Matching) 直接在特征空间中对齐合成数据与真实数据的分布，避免了优化过程中的模型训练。其典型形式为

$
  cal(S)^* = arg min_cal(S) EE_(phi.alt ~ Phi) [cal(D) (P_phi.alt (cal(S)), P_phi.alt (cal(T)))],
$

#equation-note[
  其中 $P_{phi}(\cdot)$ 表示由网络 $phi$ 提取的特征分布@zhao2023dataset。CAFE 通过多尺度特征对齐，缓解了对主导特征的过拟合问题。DANCE 则进一步结合双视图分布校准，在当时取得了最优性能@zhang2024dance。分布匹配方法通常计算效率更高，但在大规模复杂数据集上的精度仍常常落后于梯度匹配和轨迹匹配方法。
]

#algorithm(
  title: [分布匹配蒸馏],
  input: [原始数据集 $cal(T)$，每类图像数 IPC，训练轮数 $R$],
  output: [合成数据集 $cal(S)$],
  no-number[*随机初始化合成数据集 $cal(S) = {(upright(bold(s))_j, tilde(y)_j)}$*],
  no-number[*for* $r = 0, 1, dots, R-1$ *do*],
  indent[随机采样一组网络编码器 $phi$],
  indent[提取合成特征分布 $P_phi(cal(S))$],
  indent[提取真实特征分布 $P_phi(cal(T))$],
  indent[计算分布距离 $cal(L) = cal(D)(P_phi(cal(S)), P_phi(cal(T)))$],
  indent[更新合成数据 $cal(S) arrow.l cal(S) - alpha nabla_cal(S) cal(L)$],
  no-number[*end for*],
  no-number[*return* $cal(S)$],
)

=== 解耦式蒸馏方法

*解耦式方法*以 SRe2L@yin2023sre2l 系列为代表。该方法把蒸馏过程拆成“压缩、恢复、重标记”三个阶段。压缩阶段在原始数据上训练一个模型。恢复阶段通过匹配该模型的批量归一化（BN）统计量来合成数据，不需要访问原始数据。重标记阶段为合成数据生成软标签。这个框架首次在 ImageNet‑1K 上取得突破。后续工作通过自监督预训练增强 BN 统计量、引入多骨干一致性匹配以及加入真实图像初始化等方式，不断强化其规模适应性和跨架构泛化能力#multicite("zhou2024selfsupervised", "shao2024generalized", "shao2024elucidating")。SRe2L 的设计思想充分体现了把生成与标签学习分离的好处，为大规模高效蒸馏提供了新的范式。

#capfig(
  image("figures/SRe2L.png", width: 100%),
  caption: [SRe2L 方法架构图@yin2023sre2l],
  label: <fig-SRe2L-architecture>,
)

=== 基于扩散模型的生成式蒸馏方法

随着数据规模和分辨率增长，基于匹配的方法在计算和存储上的瓶颈越来越明显。生成式方法借助生成模型强大的先验能力，把数据压缩与图像生成解耦，成为近年来数据集蒸馏的主流方向之一。早期工作如 IT‑GAN 冻结预训练 GAN 的生成器，只优化潜向量和标签，强调生成样本的“训练有用性”而不是视觉逼真度。D³M 则把整类图像压缩成文本提示，利用文本反转和块选择实现高效压缩@abbasi2024d3m。扩散模型因为生成质量高、模式覆盖广，成为近期主流的生成基座。在此基础上涌现了 D⁴M、MGD³ 和 DDQO 等多个代表性方法。下面逐一介绍这三种方法的核心设计，并分析其优点和局限。

==== D⁴M：基于解耦扩散模型的蒸馏

D⁴M（Dataset Distillation via Disentangled Diffusion Model）@su2024d4m 的核心思想是通过解耦式设计，把特征聚合与图像生成分开，从而获得与架构无关、容易跨模型复用的蒸馏数据。其流程如@fig-d4m-architecture 所示。

#capfig(
  image("figures/D4M.png", width: 100%),
  caption: [D⁴M 方法架构示意图@su2024d4m],
  label: <fig-d4m-architecture>,
)

D⁴M 利用预训练变分自动编码器（Variational Autoencoder, VAE）把原始训练图像映射到潜在空间，通过 K‑Means 算法对每类特征进行聚类，提取出紧凑的类别聚类中心。这些聚类中心随后与经过文本编码器转换的标签拼接，形成条件信号。在生成阶段，模型以加噪后的聚类中心为起点进行条件去噪，逐步还原出蒸馏样本。此外，D⁴M 引入了训练时匹配机制，在生成过程中用教师网络输出的软标签来优化合成过程，进一步提升了蒸馏图像在下游分类任务中的效果。

D⁴M 的关键优势在于特征聚合与图像生成完全解耦。潜在空间中的聚类操作不依赖任何特定分类器架构，生成的聚类中心和后续合成的图像天然具有跨模型泛化能力。不过，在处理高压缩比场景时，该方法中聚类中心对数据分布的覆盖受到 K‑Means 均匀划分假设的限制，可能会遗漏频率低但判别性强的边缘模式。

==== MGD³：模式引导的多样化蒸馏

MGD³（Mode-Guided Diversified Dataset Distillation）@zhang2024m3d 同样依靠预训练扩散模型完成蒸馏，但其核心创新在于对生成路径进行显式的分布覆盖控制。@fig-mgd3-architecture 展示了其整体架构。

#capfig(
  image("figures/MGD3.png", width: 100%),
  caption: [MGD³ 方法架构示意图@zhang2024m3d],
  label: <fig-mgd3-architecture>,
)

MGD³ 在潜在空间中通过聚类定位每类的多个数据模式中心。与 D⁴M 直接以聚类中心为条件进行去噪不同，MGD³ 的采样流程侧重于显式控制生成路径的分布覆盖。在扩散去噪的早期时间步，算法将当前预测样本与目标模式中心之间的偏移量作为引导向量，叠加到噪声预测器中，强制去噪轨迹向多样化的数据子模式收敛。为了避免持续强引导带来伪影和类别失真，该方法设定了引导停止阈值。到达指定时间步后，引导信号关闭，切换回原始无条件去噪，让模型在后期自由恢复纹理与结构细节。

MGD³ 的优势在于，它生成的蒸馏样本具有更丰富的类内多样性。模式引导策略确保不同聚类中心对应的生成图像在视觉上存在可区分的差异，能够有效覆盖数据分布中的多个子模式。但它的局限是，引导强度和停止阈值的设置对最终效果影响较大，需要针对不同数据集做精细调节。另外，早期去噪步的强引导可能会引入不可逆的结构伪影。

==== DDQO：基于最优量化的蒸馏

DDQO（Dataset Distillation by Optimal Quantization）是发表在 ICLR 2026 的一种数据集蒸馏方法，其理论基础是最优量化。该方法从理论层面把解耦式数据集蒸馏重新表达为潜空间中的最优量化问题，其架构如@fig-ddqo-architecture 所示。

#capfig(
  image("figures/DDOQ.png", width: 100%),
  caption: [DDQO 方法架构示意图],
  label: <fig-ddqo-architecture>,
)

DDQO 的核心流程如下。首先，利用预训练潜在扩散模型的编码器将原始图像映射到低维潜空间。然后，对每类图像的潜特征执行 Mini-Batch K‑Means 聚类，得到 $K$ 个量化中心及其对应的 Voronoi 区域权重。这些权重由聚类过程中每个中心所分配样本的频次自动确定。在生成阶段，把带权重的潜空间量化点与类别文本嵌入拼接后，输入扩散模型的条件去噪网络，再经解码器还原为蒸馏图像。在训练学生网络时，DDQO 利用聚类权重对损失函数进行加权，使梯度更新更贴近原始数据分布的期望。

DDQO 的理论贡献在于，它证明了当量化点数 $K$ 趋于无穷时，蒸馏数据诱导的训练梯度与全量数据渐近一致，收敛速率为 $O(K^(-1/d))$（$d$ 为潜空间维度）。这一理论保证使得 DDQO 在较高 IPC 设置下具有天然的性能优势。然而，在极低 IPC 设定下，量化中心数和权重估计的统计可靠性都会明显下降，理论收敛速率的优势难以充分发挥。

综合来看，D⁴M、MGD³ 和 DDQO 从不同技术路径推进了基于扩散模型的生成式蒸馏研究。但它们在医学图像场景下仍存在一些值得改进的地方。三种方法都直接使用在大规模自然图像上预训练的扩散模型 U‑Net 作为去噪网络，没有针对医学图像特有的纹理和解剖结构做任何适配。当以医学图像的聚类中心为起点进行去噪生成时，通用去噪网络恢复的纹理和细节可能与目标医学域存在偏差，从而影响合成图像在下游分类任务中的训练效果。此外，它们的聚类策略都局限于逐类独立聚类，每类图像在潜在空间中单独做 K‑Means 聚类，完全按照类别边界划分聚类中心，无法捕捉跨类别的潜在特征共享关系。在医学图像中，不同类别的特征常常交织在一起，例如不同等级的视网膜病变在特征空间中构成连续谱。这种情况下，逐类聚类会损失宝贵的跨类别判别信息。

== 扩散模型

扩散模型是一类基于逐步去噪过程的深度生成模型。近年来，它在图像生成、图像修复等任务中表现出了很好的样本质量和多样性，超过了生成对抗网络。其核心思想是学习一个马尔可夫链，把随机噪声逐步变成符合给定分布的样本。下面介绍去噪扩散概率模型、潜在扩散模型以及参数高效微调技术LoRA，为后续将扩散模型用于医学图像蒸馏提供理论基础。

=== 去噪扩散概率模型

去噪扩散概率模型（Denoising Diffusion Probabilistic Models, DDPM）由 Ho 等人在 2020 年系统提出@ho2020denoising。它的本质是定义两个互为逆过程的多步随机变换。

前向过程（也称扩散过程）是一个固定的、不含可学习参数的马尔可夫链。它在总共 $T$ 个时间步内，逐步向原始图像 $bold(x)_0$ 中添加高斯噪声，直到图像完全变成各向同性的标准高斯分布。该过程可以写为一组条件高斯分布的乘积：

$
  q(bold(x)_(1:T) | bold(x)_0) = product_(t=1)^T q(bold(x)_t | bold(x)_(t-1)),
$

$
  q(bold(x)_t | bold(x)_(t-1)) = cal(N) (bold(x)_t; sqrt(1 - beta_t) bold(x)_(t-1), beta_t bold(I)),
$

#equation-note[
  其中 $beta_t in (0, 1)$ 为预先定义的方差调度序列（variance schedule），控制每一步注入的噪声强度。引入记号 $alpha_t = 1 - beta_t$ 及 $macron(alpha)_t = product_(s=1)^t alpha_s$，利用高斯分布的可加性，可以将 $bold(x)_t$ 直接由 $bold(x)_0$ 解析地表示：
]

$
  q(bold(x)_t | bold(x)_0) = cal(N) (bold(x)_t; sqrt(macron(alpha)_t) bold(x)_0, (1 - macron(alpha)_t) bold(I)),
$

#equation-note(prefix: none)[
  上式揭示了扩散模型的核心特性：任意时刻 $t$ 的带噪图像均可通过从 $bold(x)_0$ 出发的单步采样获得：
]

$
  bold(x)_t = sqrt(macron(alpha)_t) bold(x)_0 + sqrt(1 - macron(alpha)_t) epsilon, quad epsilon tilde cal(N)(0, bold(I)).
$

反向过程是前向过程的逆转，同样建模为一个马尔可夫链，但每一步的高斯转移由参数化的神经网络（通常为 U‑Net 结构）来近似：

$
  p_theta(bold(x)_(t-1) | bold(x)_t) = cal(N) (bold(x)_(t-1); bold(mu)_theta(bold(x)_t, t), sigma_t^2 bold(I)),
$

#equation-note[
  其中 $bold(mu)_theta$ 为网络输出的均值估计，$sigma_t^2$ 为固定的时间步相关方差。在实践中，Ho 等人发现直接预测所添加的噪声 $epsilon$ 比预测均值更为稳定，因此令网络 $epsilon_theta(bold(x)_t, t)$ 以带噪图像和当前时间步为输入，输出对噪声的估计。训练时，通过最小化真实噪声与网络预测噪声之间的均方误差来优化参数，简化后的训练目标为
]

$
  cal(L)_"simple" = bb(E)_(t, bold(x)_0, epsilon) [|| epsilon - epsilon_theta(bold(x)_t, t) ||^2],
$

#equation-note[
  其中时间步 $t$ 从 ${1, ..., T}$ 中均匀采样。一旦训练完成，从纯噪声 $bold(x)_T tilde cal(N)(0, bold(I))$ 出发，通过逐步执行反向去噪步骤 $t = T, T-1, ..., 1$，便可生成全新的样本。
]

DDPM 的出现为生成高质量图像提供了稳定的范式，但其推理过程需要经历完整的 $T$ 步串行计算（通常 $T = 1000$），生成速度较慢，且在高分辨率图像上的计算和内存开销极大。

@fig-unet-architecture 展示了扩散模型中去噪网络所采用的 U‑Net 架构。U‑Net 是一种编码器-解码器结构的全卷积网络，因其对称的"U"形特征图尺寸变化而得名。编码器路径（左侧）通过逐级下采样逐步压缩空间分辨率、扩大通道数，从而提取从局部纹理到全局语义的多尺度特征；解码器路径（右侧）通过逐级上采样恢复空间分辨率，并与编码器的对应层级通过跳跃连接（skip connection）融合，以保留在下采样过程中丢失的细粒度空间细节。在扩散模型中，时间步 $t$ 通过正弦位置编码后嵌入到各层，为网络提供当前的噪声水平信息，使其可以针对不同噪声强度采取不同的去噪策略。

#capfig(
  image("figures/unet.png", width: 100%),
  caption: [扩散模型中使用的 U‑Net 去噪网络架构示意图],
  label: <fig-unet-architecture>,
)

=== 潜在扩散模型

为了缓解直接在像素空间做扩散带来的计算负担，Rombach 等人提出了潜在扩散模型（Latent Diffusion Models, LDM）@rombach2022high。它的核心改进是把扩散过程从高维的像素空间，迁移到一个低维的潜在空间。这个潜在空间由预训练的自编码器提取。

具体来说，LDM 利用冻结的自编码器把图像压缩成紧凑的潜在表示：

$
  bold(z)_0 = cal(E)(bold(x)), quad bold(x) = cal(D)(bold(z)_0),
$

#equation-note[
  其中 $cal(E)$ 为编码器，$cal(D)$ 为解码器。该潜在表示保留了足够的语义和结构信息，同时大幅降低了空间维度。随后，扩散与去噪过程全部在潜在空间中进行：
]

$
  cal(L)_"LDM" = bb(E)_(cal(E)(bold(x)), epsilon, t) [|| epsilon - epsilon_theta(bold(z)_t, t) ||^2],
$

#equation-note[
  其中前向扩散和逆向去噪的数学形式与 DDPM 完全一致，仅将操作对象从像素 $bold(x)_t$ 替换为潜在变量 $bold(z)_t$。生成时，从潜在空间采样一个噪声向量 $bold(z)_T tilde cal(N)(0, bold(I))$，通过逆向过程获得去噪后的潜在表示 $bold(z)_0$，最后利用自编码器的解码器 $cal(D)$ 将其映射回像素空间。
]

得益于潜在空间的低维特性，LDM 大幅降低了训练和推理的计算需求，可以在单块 GPU 上完成高分辨率图像的生成。此外，LDM 可以通过交叉注意力机制灵活引入文本、图像、语义图等条件信号。设 $bold(c)$ 为条件输入（比如文本描述的嵌入向量），交叉注意力操作在 U‑Net 的中间层，让去噪网络的特征与条件信息进行交互：

$
  "Attention"(bold(Q), bold(K), bold(V)) = "softmax"((bold(Q) bold(K)^top) / sqrt(d)) bold(V),
$

#equation-note[
  其中 $bold(Q)$ 来自去噪网络的特征映射，$bold(K)$ 和 $bold(V)$ 来自条件编码器对 $bold(c)$ 的投影。这一机制使 LDM 成为一种高度可扩展的条件生成框架，为后续将其应用于数据集蒸馏提供了有力工具。
]

@fig-cross-attention 进一步以示意图的形式展示了交叉注意力机制在 U‑Net 内部的工作流程。在每一个交叉注意力层中，来自 U‑Net 中间特征图的空间位置被线性投影为查询向量 $bold(Q)$，而条件信息（如医学图像的类别文本描述）经文本编码器编码后投影为键向量 $bold(K)$ 和值向量 $bold(V)$。通过查询与键的点积注意力计算，网络可以在每个空间位置上自适应地聚合与之最相关的条件语义，从而实现文本引导的图像生成。在本文的蒸馏框架中，正是这一机制使得聚类中心引导的去噪过程可以根据类别提示词生成语义一致的高质量医学蒸馏图像。

#capfig(
  image("figures/LDM.png", width: 80%),
  caption: [潜在扩散模型中交叉注意力机制的示意图。查询 $bold(Q)$ 来自 U‑Net 特征图，键 $bold(K)$ 和值 $bold(V)$ 来自条件文本编码器，通过注意力权重实现文本条件对图像生成过程的引导。],
  label: <fig-cross-attention>,
)

== 参数高效微调与 LoRA

把通用的大规模扩散模型适配到特定领域（比如医学图像）时，全量微调会带来很大的计算和存储负担，而且容易过拟合。参数高效微调技术只调整极少量的额外参数，让模型能够适应新领域，同时保留预训练阶段学到的丰富先验。

低秩适应（LoRA）@hu2022lora 是其中最有代表性的方法之一。它最初是为大语言模型提出的，后来也被广泛用于扩散模型。LoRA 的核心假设是，模型在适配新任务时，其权重更新的矩阵具有低秩特性。因此，对于模型中的某一线性层，不直接修改其原始权重矩阵 $bold(W)_0 in bb(R)^(d times k)$，而是引入一对可训练的分解矩阵 $bold(B) in bb(R)^(d times r)$ 和 $bold(A) in bb(R)^(r times k)$，其中秩 $r << min(d, k)$。前向传播的计算形式化为

$
  bold(h) = bold(W)_0 bold(x) + bold(B) bold(A) bold(x) .
$

#equation-note(prefix: none)[
  在微调过程中，原始权重 $bold(W)_0$ 被冻结，仅更新 $bold(A)$ 和 $bold(B)$。由于 $r$ 远小于原始维度，LoRA 引入的可训练参数量非常小，而且可以在推理时通过重参数化把低秩增量合并回原权重，不会增加额外的推理延迟。
]

在扩散模型中，LoRA 通常被插入到 U‑Net 或 Transformer 骨干网络的注意力层里，使其能在极少量样本上快速适应新领域的视觉风格和内容结构。本文后续将用 LoRA 对预训练的潜在扩散模型进行面向医学图像数据集的微调，为蒸馏阶段提供准确的潜在表示生成能力。

== 聚类算法

聚类是一种无监督学习方法。它的目标是把数据集中的样本按照某种相似度划分成若干个互不相交的子集（称为簇），使得同一簇内的样本彼此高度相似，不同簇之间的样本差异尽可能大。在数据集蒸馏中，聚类被用来从大规模原始数据的特征表示中提取紧凑的聚类中心，从而用极少的代表点概括整个数据分布的结构。

=== K-Means 聚类

K‑Means 是最经典、应用最广泛的基于划分的聚类算法之一。给定 $n$ 个样本点 ${bold(x)_1, bold(x)_2, ..., bold(x)_n}$ 和预设的簇数 $K$，算法通过迭代优化将每个样本分配到距离最近的簇中心，并不断更新簇中心的位置，最终收敛到一个局部最优的划分。

算法的优化目标是最小化簇内平方误差和，即所有样本到其所属簇中心的欧氏距离平方之和：

$
  "WCSS" = sum_(k=1)^K sum_(bold(x)_i in C_k) || bold(x)_i - bold(mu)_k ||^2,
$

#equation-note[
  其中 $C_k$ 表示第 $k$ 个簇，$bold(mu)_k$ 为簇 $C_k$ 的质心（即簇内所有样本的均值）。
]

K‑Means 的标准求解采用 Lloyd 算法，迭代执行两步，直到簇分配不再变化或达到最大轮次。第一步是分配步，把每个样本分配到离它最近的质心。第二步是更新步，把质心更新为当前簇中所有样本的均值。该算法的时间复杂度为 $O(n K d I)$，其中 $d$ 为特征维度，$I$ 为迭代次数。

#algorithm(
  title: [标准 K-Means 聚类（Lloyd 算法）],
  input: [数据集 $cal(D) = {bold(x)_1, ..., bold(x)_n}$，簇数 $K$，最大迭代次数 $I_max$],
  output: [簇分配 ${C_1, ..., C_K}$ 及质心 ${bold(mu)_1, ..., bold(mu)_K}$],
  no-number[*随机初始化 $K$ 个质心 $bold(mu)_1, ..., bold(mu)_K$*],
  no-number[*for* $t = 1, 2, dots, I_"max"$ *do*],
  indent[分配步：将每个 $bold(x)_i$ 分配到最近质心所在簇],
  indent(indent[$C_k = {bold(x)_i | k = "argmin"_j || bold(x)_i - bold(mu)_j ||^2}$]),
  indent[更新步：重新计算各簇质心],
  indent(indent[$bold(mu)_k = 1/(|C_k|) sum_(bold(x)_i in C_k) bold(x)_i$]),
  indent[*if* 质心收敛（变化小于阈值）*then break*],
  no-number[*end for*],
  no-number[*return* $C_1, ..., C_K$, $bold(mu)_1, ..., bold(mu)_K$],
)

K‑Means 算法因为原理简单、实现快、收敛快，被广泛使用。但它也有几个固有的缺点。它对初始质心比较敏感，随机初始化可能会收敛到局部最优。它需要提前指定簇数 $K$，选得不合适会影响聚类质量。另外，在欧氏空间中更新质心时，容易受离群点和噪声的影响。

在数据集蒸馏中，K‑Means 常被用来提取聚类中心。先用预训练的特征提取器把高维特征向量映射出来，再做聚类，把得到的 $K$ 个质心作为数据分布的紧凑概要，用来替代原始的大量样本参与下游训练。本文采用 K‑Means 的改进版本 MiniBatchKMeans，以进一步提高计算效率。

=== 小批量 K-Means 聚类

MiniBatchKMeans 是 Sculley 于 2010 年提出的 K‑Means 在线小批量版本。它在保持聚类质量的同时，明显降低了计算和内存开销。其核心思想是，不在完整数据集上做全量迭代，而是每一步只在一个小批量随机子集上更新质心。

具体来说，算法在每轮迭代中随机抽取 $b$ 个样本组成一个小批量，对这个批量执行分配和更新操作，并用指数衰减的学习率逐步更新质心，最终收敛到稳定解。第 $t$ 轮时，小批量质心的更新规则为：

$
  bold(mu)_k^((t+1)) = bold(mu)_k^((t)) + eta_t dot.op 1/(n_k^"batch") sum_(bold(x)_i in C_k^"batch") (bold(x)_i - bold(mu)_k^((t))),
$

#equation-note[
  其中 $C_k^"batch"$ 为当前小批量中被分配到第 $k$ 个簇的样本集合，$n_k^"batch" = |C_k^"batch"|$，$eta_t = 1 / (t + 1)$ 为当前步的学习率（实践中常采用 $1 / (n_k^"batch" + 1)$ 等更平滑的衰减形式）。该更新可被视作一种随机梯度下降：每步仅使用少量样本的梯度估计更新质心，避免了全量数据的逐样本扫描。
]

#algorithm(
  title: [MiniBatchKMeans 聚类],
  input: [数据集 $cal(D)$，簇数 $K$，批量大小 $b$，迭代轮数 $T$],
  output: [质心 ${bold(mu)_1, ..., bold(mu)_K}$],
  no-number[*随机初始化质心 $bold(mu)_1, ..., bold(mu)_K$*],
  no-number[*for* $t = 1, 2, dots, T$ *do*],
  indent[随机抽取小批量 $cal(M) subset cal(D)$，$|cal(M)| = b$],
  indent[将 $cal(M)$ 中每个样本分配到最近质心],
  indent[*for* 每个簇 $k = 1, ..., K$ *do*],
  indent(indent[计算学习率 $eta_t = 1 / (n_k^"batch" + 1)$]),
  indent(
    indent[更新质心 $bold(mu)_k arrow.l (1 - eta_t) bold(mu)_k + eta_t dot.op (1 / n_k^"batch") sum_(bold(x)_i in C_k^"batch") bold(x)_i$],
  ),
  indent[*end for*],
  no-number[*end for*],
  no-number[*return* ${bold(mu)_1, ..., bold(mu)_K}$],
)

与标准 K‑Means 相比，MiniBatchKMeans 的优势主要体现在三个方面。第一，计算效率高。每轮只处理少量样本，而不是全量数据集，在大规模数据上运行时间可减少数倍甚至数十倍，内存占用也稳定可控。第二，具有在线增量特性。只需遍历一次数据就能得到收敛解，非常适合数据流场景或数据量太大无法一次性载入内存的情况。第三，随机梯度更新有助于跳出局部最优。小批量采样带来的随机性为优化过程注入了噪声，可以帮助质心摆脱浅层的局部最优。

在聚类质量方面，大量实验表明，在合适的批量大小下，MiniBatchKMeans 的聚类结果与标准 K‑Means 非常接近。在 IPC 极低的数据集蒸馏场景中，它的效率优势更为突出。

本文在潜在空间全局聚类阶段选用 MiniBatchKMeans 算法，主要基于以下考虑。医学图像经过 VAE 编码后的潜在特征维度高、样本量大，标准 K‑Means 每次迭代都要扫描全部样本来更新质心，计算开销较大。而 MiniBatchKMeans 以恒定大小的批次为单位就能完成全局聚类，同时保留了数据分布的主要结构信息。

== 数据集蒸馏中的软标签策略

在数据集蒸馏中，合成数据集的规模通常非常小，每个类别只有极少几张合成图像。如果只用硬标签来训练模型，模型很容易死记硬背这些蒸馏样本，从而丧失泛化能力。为了解决这个问题，研究者引入了软标签机制。用一个预训练好的、性能较强的教师模型，为每张合成图像生成一个类别概率分布，作为监督信号，替代传统的独热硬标签。其核心想法是，神经网络最后一层输出的类别概率分布（即 logit 经过 softmax 后的结果）不仅包含真实标签的信息，还编码了类别之间的相似性结构。这种“暗知识”是传统的独热硬标签无法充分传达的@hinton2015distilling。

在软标签生成过程中，教师模型的输出分布通常通过引入温度参数 $T > 1$ 来软化：

$
  p_i^T = frac(e^((z_i / T)), sum_j e^((z_j / T))),
$

#equation-note[
  其中 $z_i$ 为教师模型输出的第 $i$ 类 logit。当 $T = 1$ 时，该分布退化为普通的 softmax 输出；当 $T$ 增大时，分布变得更加平滑，各个错误类别的概率相对凸显，从而传递更丰富的类间关系。
]

SRe2L 框架把软标签策略深度整合到其解耦式蒸馏流程中@yin2023sre2l。该框架把蒸馏过程分成“压缩、恢复、重标记”三个阶段。其中的“重标记”阶段，本质上就是利用教师模型为恢复得到的合成数据生成软标签，从而把教师知识迁移到学生模型。SRe2L 的实验表明，软标签携带了丰富的类间关系信息，因此在极低 IPC 设定下仍能获得与全量数据训练相近的性能，这充分验证了软标签策略在数据集蒸馏中的核心地位。后续的 SC-DD@zhou2024selfsupervised 和 EDC@shao2024elucidating 等工作也沿用了类似的软标签机制，进一步巩固了软标签作为数据集蒸馏关键组件的共识。

受上述工作的启发，本文在数据集蒸馏的训练阶段也采用软标签策略。具体做法是，用在原始医学图像数据集上预训练的教师模型，为蒸馏图像生成软标签作为监督信号，替代传统的硬标签。这样，学生模型在训练样本极少的情况下，仍然能够学到不同类别之间的细微差别，在兼顾数据隐私和训练效率的同时，获得较好的分类精度。

== 本章小结

本章围绕本文方法所涉及的基础理论与关键技术，按照“数据集蒸馏范式→扩散模型→LoRA 微调→聚类算法→软标签策略”的顺序，系统阐述了后续方法设计的理论支撑。

从数据集蒸馏最原始的双层优化形式化定义入手，梳理了经典迭代方法（BPTT、RaT‑BPTT、Teddy）的优化演进，以及梯度匹配、轨迹匹配、分布匹配三种主流匹配范式的核心思想与适用边界。随后重点介绍了以 SRe2L 为代表的解耦式方法，以及以 D⁴M、MGD³、DDQO 为代表的基于扩散模型的生成式蒸馏方法，并逐一分析了各方法在医学图像场景下的局限性，主要包括域适配缺失、逐类独立聚类导致跨类别判别信息损失等。由此引出了本文的改进动机，即 LoRA 域适配与全局聚类。

在生成模型方面，本章从 DDPM 的前向扩散与反向去噪过程出发，推导了噪声预测的简化训练目标。接着介绍了潜在扩散模型（LDM）如何通过把扩散过程迁移到低维潜在空间来降低计算开销，以及交叉注意力机制如何实现文本条件引导生成。然后介绍了参数高效微调技术 LoRA 的数学原理和插入方式。LoRA 通过在注意力层注入低秩分解矩阵，只用极少的参数量就能完成大模型的微调，这为后续面向医学图像的扩散模型微调提供了理论基础。

在数据压缩与训练监督方面，本章详细阐述了聚类算法中 K‑Means 及其小批量变体 MiniBatchKMeans 的原理与算法流程。从最小化簇内平方误差和的优化目标出发，推导了 Lloyd 算法中分配与更新两步交替的机制，并分析了 MiniBatchKMeans 通过小批量随机梯度更新在计算效率、在线增量特性和逃离局部最优等方面的优势，为第三章中潜在空间全局聚类策略提供了算法基础。最后阐述了软标签在数据集蒸馏中的作用机制。通过引入温度参数对教师模型输出进行平滑处理，软标签可以编码类别之间的相似性信息，弥补极低 IPC 条件下硬标签监督信号不足的问题，为学生模型的训练提供更丰富的语义指导。

= 基于扩散模型与全局聚类的医学图像蒸馏方法

== 方法整体框架

本文提出的基于扩散模型的医学图像数据集蒸馏方法，旨在降低存储和计算开销，生成一个信息密度高的合成数据集，学生模型使用合成数据集训练后，性能可以达到或接近原始全量数据训练的结果。整体框架如@framework 所示，供分为三个阶段：扩散模型微调、潜在空间聚类生成蒸馏数据和软标签训练。

#capsubfig(
  (
    (content: image("figures/framework-1.png", width: 95%), subcaption: [第一阶段]),
    (content: image("figures/framework-2.png", width: 95%), subcaption: [第二、三阶段]),
  ),
  columns: 1,
  caption: [整体框架概览],
  label: <framework>,
)


第一阶段以预训练扩散模型为基础，在目标医学图像数据集上使用低秩适应（LoRA）技术进行参数高效微调，使模型可以生成符合医学图像统计特性的潜在表示，为后续生成蒸馏图像提供高质量的生成先验。

第二阶段通过冻结的 VAE 编码器将原始训练图像映射至潜在空间，在所有类别的潜在特征上执行全局聚类，得到聚类中心。随后，以加噪的聚类中心为起点，通过经 LoRA 微调后的 U‑Net 去噪网络执行反向去噪过程，生成最终的蒸馏图像。

第三阶段利用原始全量数据集训练一个教师模型，再使用该模型对第二阶段生成的蒸馏图像进行推理，得到平滑的软标签。学生模型训练时，使用蒸馏图片和软标签进行训练。通过这种机制，下游的分类模型可以在数据量极度压缩的情况下从蒸馏图片和软标签中学到分类知识，实现高效的医学影像分类。

LoRA 微调后的扩散模型提供了面向医学图像领域的图片生成能力；在原始数据集上全局聚类再去噪生成蒸馏数据集可以保证视觉语义的同时，保留了类别判别力；软标签训练进一步的让下游分类模型学习到教师模型丰富的暗知识，提供分类性能，最终完成高效的医学影像分类任务。本章的后续小节将对各模块的设计动机、数学表达和具体实现进行详细的叙述。

== 扩散模型微调

很多主流的大规模预训练扩散模型都在自然图像上训练，比如 LAION-5B。但医学图像不一样，X 光、CT、病理切片这些影像有自己特殊的纹理和解剖结构，跟自然图像差别很大。如果直接用通用扩散模型去生成蒸馏图像，合成出来的图容易缺少关键的医学特征，这样下游分类任务的效果就会受影响。全量微调做起来简单，效果也不错，可它的计算和存储成本太高，训练时还特别容易过拟合。所以我们用低秩适应（LoRA）来做参数高效的域适配，只需要很少的参数和计算资源就能微调扩散模型。

我们选的预训练扩散模型是 Stable Diffusion v1.5，它是一种潜在扩散模型（LDM）。LDM 不直接在像素空间里做扩散，而是转到潜在空间。这个潜在空间由一个自编码器压缩得到。这样做计算量小很多，同时还能保证生成质量。另外，LDM 用交叉注意力机制把文本信息加进来，在去噪过程中用文本语义来引导图像生成。所以 LDM 给后面的蒸馏图像合成提供了一个既灵活又高效的框架。

LoRA 的核心想法是：在模型的某些层里加入可训练的低秩分解矩阵，原来的预训练权重保持不变。对于扩散模型，LoRA 通常加在 U‑Net 骨干网络的交叉注意力层。具体做法是：注意力模块里的查询、键、值以及输出线性投影层，每一处都引入低秩分解。微调时，预训练权重全部冻结，只更新这些低秩矩阵。这样参数量通常不到原模型的千分之一。这种设计的好处是：既保留了大模型从自然图像中学到的丰富结构和纹理知识，又能让它很快适应医学图像特有的灰度分布和形态模式。

我们的域适配过程是：输入目标医学图像数据集，用去噪扩散训练目标对模型进行 LoRA 微调。具体到潜在空间里的表示，训练时会随机采样时间步和标准高斯噪声，然后最小化预测噪声与真实噪声之间的均方误差，以此优化 LoRA 参数。微调用的是 AdamW 优化器，学习率采用余弦退火调度，再加上一个预热阶段。训练时还用了类别平衡采样，用来缓解医学数据中常见的类别不均衡问题。同时，以一定概率随机丢弃类别提示词，这样模型在没有文本条件时也能生成得比较稳健。另外，损失函数里加了基于信噪比的加权策略，帮助模型在不同噪声水平下平衡梯度更新的贡献。

训练结束后，这个模型可以把医学图像编码成带有丰富语义的潜在特征，也能通过反向去噪过程从潜在空间生成图像。生成的图像既真实，又保留了医学数据里的统计特性。这个适配模型为下一步的全局聚类和蒸馏图像生成提供了质量不错的生成基础。

#algorithm(
  title: [LoRA 域适配微调],
  input: [目标医学数据集 $cal(T)$，预训练扩散模型 $epsilon_theta$（权重冻结），LoRA 秩 $r$，训练轮数 $E$],
  output: [微调后的 LoRA 权重 $Delta Theta = {bold(A)_l, bold(B)_l}$],
  no-number[*在 U‑Net 注意力层的 $bold(Q), bold(K), bold(V), bold(O)$ 投影中插入 LoRA 低秩矩阵 $bold(B)_l bold(A)_l$*],
  no-number[*初始化 $bold(A)_l tilde cal(N)(0, sigma^2)$, $bold(B)_l = 0$*],
  no-number[*for* $e = 0, 1, dots, E-1$ *do*],
  indent[从 $cal(T)$ 中按类别平衡采样一个批次 $cal(B) = {(bold(x)_i, y_i)}$],
  indent[编码为潜在表示 $bold(z)_0 = cal(E)(bold(x))$],
  indent[随机采样时间步 $t tilde cal(U)({1, dots, T})$ 和噪声 $epsilon tilde cal(N)(0, bold(I))$],
  indent[计算带噪潜在表示 $bold(z)_t = sqrt(macron(alpha)_t) bold(z)_0 + sqrt(1 - macron(alpha)_t) epsilon$],
  indent[以概率 $p_"uncond"$ 随机丢弃类别提示词 $bold(c)$],
  indent[计算去噪损失 $cal(L) = \|epsilon - epsilon_(theta, Delta Theta)(bold(z)_t, t, bold(c))\|^2$],
  indent[更新 LoRA 权重 $Delta Theta arrow.l Delta Theta - eta nabla_(Delta Theta) cal(L)$],
  no-number[*end for*],
  no-number[*return* $Delta Theta$],
)

== 潜在空间全局聚类和蒸馏图像生成

在完成扩散模型的医学图像域适配之后，我们得到一个潜在空间。这个空间能比较准确地编码医学图像特征。跟直接在像素空间操作不一样，潜在空间是低维的、紧凑的，语义上也更连续。所以它更适合做高效的信息压缩和结构化建模。当前很多其他基于扩散模型的蒸馏方法都是按照类别逐个聚类，这样可以确定聚类中心的类别标签，但我认为强制按照类别划分进行聚类，会丢失一部分处于不同类别边界处的信息，本文的一个核心创新点就是在潜在空间里对所有类别做一次全局聚类，这样可以最大程度的保留原始信息。

此外医学图像数据本身常常存在明显的类内和类间变异。同一个病灶，大小、形状、纹理、位置都可能不同。不同类别的图像之间，视觉特征也可能有重叠。很多医学图像数据集还有严重的类别不平衡问题：正常样本占绝大多数，某些罕见病的阳性样本极少。如果按每类固定数量的聚类中心去做逐类独立聚类，少数类因为样本不够，它的聚类中心就容易过度拟合个别噪声点，代表性不够。反过来，对于多数类，大量的样本被压缩到同样数量的聚类中心里，导致很多有价值的类内多样性被丢掉了。并且如果只在每个类别内部独立聚类，就没法捕捉跨类别之间的特征共享关系。在类别不平衡的情况下，这种做法会同时让少数类信息失真，也让多数类信息浪费。相比之下，全局聚类策略不会为每个类预设聚类中心数量的配额。它完全根据特征空间的自然密度分布来划分簇：样本密集的区域自动生成更多聚类中心，充分覆盖类内多样性；样本稀疏的区域就用较少聚类中心覆盖。然后再通过多数投票确定每个簇属于哪个类别。这样一来，一个聚类中心可以聚合来自不同类别但相似的潜在特征，捕捉跨类别的共享关系。同时，聚类中心分布也能跟数据密度自适应地匹配。所以，在数据量被高度压缩的条件下，这种方法能更均衡地保留各个类别的判别信息。

=== 潜在特征提取

利用预训练且冻结的潜在扩散模型 VAE 编码器，这个模块在之前的阶段并没有微调，因为考虑到 VAE 编码器作为一个通用视觉特征提取的模块，微调意义不大。将所有图像映射到潜在空间后，得到每个样本的潜在特征向量。编码后的特征一起被送入聚类模块。

=== 全局聚类与聚类中心生成

与其他方法的按类别独立聚类不同，我们把所有训练样本的潜在特征当成一个整体，直接做全局聚类。这样做的目的是捕捉数据分布里的主要模式。用的算法是 MiniBatchKMeans，它在欧氏空间里迭代优化簇内的平方误差来分组。这个算法每次只用一小批样本来更新聚类中心，在大规模数据上计算效率明显更高。

聚类结束以后，每个簇里面可能包含来自不同类别的潜在特征。为了给每个聚类中心定一个类别，我们对簇里所有样本的原始标签做多数投票：统计一下簇里每个类别的样本出现次数，选出现最多的那个标签作为这个聚类中心的最终类别。这样得到的每个聚类中心都代表了数据集中某一类典型的潜在特征模式。这个模式可能主要来自某一个类别，也可能把多个类别的共享特征聚在一起。

总共有多少个聚类中心，由每类预设的聚类中心数乘以总类别数来决定。这个参数直接决定了蒸馏数据集的压缩程度：聚类中心数越多，合成出来的数据集规模越大，保留下来的信息也越多；聚类中心数越少，压缩率越高，训练效率提升越明显。另外，我们还做了两种替代策略作为实验对比的基线，一种是随机选取，另一种是 K‑Means 近邻选取。这两种方法不用扩散模型去生成，而是直接从原始训练集里挑真实图像作为蒸馏样本。这样就能对比出生成式蒸馏和选取式蒸馏在医学图像场景下的性能差别。

=== 基于聚类中心的蒸馏图像生成

得到每个类别在潜在空间里的聚类中心后，我们用扩散模型的反向去噪过程，把这些低维、语义紧凑的聚类中心恢复成图像。整个过程都在潜在空间里完成，既能保证生成质量，计算效率也高。

具体步骤是这样。给一个聚类中心，按事先设好的噪声强度往里加噪声，得到一个带噪的潜在表示。然后从这个带噪状态出发，做多步逆向去噪。每一步里，都用之前 LoRA 微调过的去噪网络去预测当前的噪声分量，并一步步去掉它。类别提示词（比如病灶的名字或描述）通过交叉注意力注入到网络里，让生成结果在语义上往这个类别靠拢。

走完整个去噪过程之后，我们得到一个接近干净的潜在表示。最后用预训练的潜在扩散模型解码器，把这个潜在表示映射回像素空间，就得到了最终的蒸馏图像。这张图像既保留了聚类中心所代表的特征模式的语义信息，又借助扩散模型在自然图像上学到的先验，保持了视觉上的真实感。这样学生模型学到的特征会更有泛化性。蒸馏数据集里总共有多少张图像，由聚类中心的数量决定，默认情况下，每个聚类中心通过扩散模型的反向去噪过程生成一张蒸馏图像。

通过这种聚类中心引导的去噪方法，我们能在极低的 IPC 设定下，生成紧凑又高质量的医学蒸馏图像。每一张图像都忠实地代表了数据分布里某一种典型模式，为后面的软标签训练打下了扎实的基础，整个过程如@label 所示。

#algorithm(
  title: [潜在空间全局聚类与蒸馏生成],
  input: [原始训练集 $cal(T)$，冻结的 VAE 编码器 $cal(E)$，每类图像数 IPC，总类别数 $C$，去噪步数 $K$，噪声强度 $sigma$],
  output: [蒸馏数据集 $cal(S) = {(bold(s)_j, tilde(y)_j)}$],
  no-number[\// 阶段 1: 潜在特征提取],
  no-number[*for* 每个 $(bold(x)_i, y_i) in cal(T)$ *do*],
  indent[提取潜在特征 $bold(z)_i = cal(E)(bold(x)_i)$，记录标签 $y_i$],
  no-number[*end for*],
  no-number[\// 阶段 2: 全局聚类],
  no-number[将全部 $bold(z)_i$ 送入 MiniBatchKMeans，生成 $N = C times "IPC"$ 个聚类中心],
  no-number[*for* 每个聚类中心 $bold(mu)_k$ *do*],
  indent[统计簇内样本标签频率，取多数投票 $tilde(y)_k = "argmax"_c thin "freq"(c)$],
  indent[令聚类中心 $bold(p)_k = bold(mu)_k$，类别 $tilde(y)_k$],
  no-number[*end for*],
  no-number[\// 阶段 3: 聚类中心引导去噪生成],
  no-number[*for* 每个聚类中心 $(bold(p)_k, tilde(y)_k)$ *do*],
  indent[加噪获得初始状态 $bold(z)_(K) = sqrt(1 - sigma^2) bold(p)_k + sigma epsilon$, $epsilon tilde cal(N)(0, bold(I))$],
  indent[*for* $k = K, K-1, dots, 1$ *do*],
  indent(indent[$bold(z)_(k-1) = "DDIMStep"(bold(z)_k, k, bold(c)_(tilde(y)_k), epsilon_theta)$]),
  indent[*end for*],
  indent[解码 $bold(s)_k = cal(D)(bold(z)_0)$],
  no-number[*end for*],
  no-number[*return* $cal(S) = {(bold(s)_k, tilde(y)_k)}_(k=1)^N$],
) <label>

== 教师-学生模型软标签训练

生成的蒸馏数据集无法直接拿去训练，因为在全局聚类下他们是缺少标签的，因此我采用了一种简单的做法，使用教师模型对蒸馏图片进行标记，这样生成的蒸馏数据集可以视作 （图片，软标签）二元组，下游的分类模型在软标签的指导下可以正常训练。

=== 教师模型预训练

使用原始医学图像训练集训练一个教师模型。教师模型的网络架构采用 ResNet-18，加载 ImageNet 上的预训练权重，在完整的训练数据上，用标准的交叉熵损失去优化，直到模型收敛。教师模型学到的知识，后面会作为学生模型学习的主要监督来源。训练完之后，教师模型的参数就冻结了，不参与后面的训练。

=== 软标签生成

蒸馏数据集里的每一张合成图像，都输入到教师模型里，得到每个类别的预测分数。为了把教师模型里关于类别之间相似性的“暗知识”提取出来，我们用一个带温度参数的 softmax 函数来生成软标签。温度设高一点，教师输出的概率分布就会更平滑，那些错误类别的相对概率会变大，从而包含更丰富的类间结构信息。如果温度参数设成 1，那就退化成了普通的 softmax 输出。

=== 软标签训练

学生模型也使用的 ResNet-18，同样用 ImageNet 上预训练的权重做初始化。训练时只用蒸馏数据集作为输入。训练的目标是最小化学生输出的分布与教师软标签之间的交叉熵：

$
  cal(L) = T^2 dot.op 1/(|cal(S)|) sum_(i = 1)^(|cal(S)|) (- sum_(j = 1)^C p_{i,j}^T log p_{i,j}^S),
$

#equation-note[
  其中 $p_{i,j}^T$ 为教师软标签，$p_{i,j}^S$ 为学生输出经相同温度软化后的概率分布，$C$ 为总类别数。前面的 $T^2$ 项用来补偿温度缩放对梯度大小的影响。训练学生模型时，我们用 AdamW 优化器和余弦退火学习率调度。
]

通过这种软标签训练，学生模型能比较好地继承教师模型的判别信息，在数据量被极度压缩的情况下，仍然能达到不错的分类性能。

#algorithm(
  title: [教师-学生软标签训练],
  input: [蒸馏数据集 $cal(S)$，温度 $tau$，学生训练轮数 $E_s$，教师模型参数 $theta_T$],
  output: [训练好的学生模型参数 $theta_S$],
  no-number[初始化学生 ResNet-18 参数 $theta_S$（ImageNet 预训练权重）],
  no-number[*for* $e = 0, 1, dots, E_s-1$ *do*],
  indent[*for* 每个批次 $cal(B) subset cal(S)$ *do*],
  indent(indent[对 $bold(s)_j$ 执行数据增广得到 $tilde(bold(s))_j$]),
  indent(indent[教师生成软标签 $bold(p)_j = "softmax"(f_(theta_T)(tilde(bold(s))_j) / tau)$]),
  indent(indent[学生前向传播 $bold(q)_j = "softmax"(f_(theta_S)(tilde(bold(s))_j) / tau)$]),
  indent(indent[计算交叉熵损失 $cal(L) = tau^2 / (|cal(B)|) sum_j (- sum_c p_(j,c) log q_(j,c))$]),
  indent(indent[更新学生参数 $theta_S arrow.l theta_S - eta nabla_(theta_S) cal(L)$]),
  indent[*end for*],
  no-number[*end for*],
  no-number[*return* $theta_S$],
)

== 本章小结

本章提出一种基于扩散模型与全局聚类的医学图像蒸馏方法，整体由域适配、信息压缩与知识迁移三个阶段构成。

在域适配阶段，基于预训练潜在扩散模型（Stable Diffusion v1.5）引入低秩适应（LoRA）进行参数高效微调，以较小的可训练参数量获得医学图像域的潜在表征能力，为后续蒸馏提供高质量生成先验。

在信息压缩阶段，利用 VAE 编码得到的潜在特征对全体样本进行 MiniBatchKMeans 全局聚类，并以多数投票为聚类中心赋予类别标签。随后以聚类中心为起点执行反向去噪生成蒸馏图像，从而在高压缩率下缓解类内变异、类间重叠与类别不平衡问题并保留判别信息。

在知识迁移阶段，采用教师‑学生软标签训练框架，以温度软化的概率分布传递类别间相似性，使学生模型在数据量被极度压缩的情况下仍能获得较好的分类性能。整体方法无需额外标注成本即可实现数据与计算的高效压缩，为后续实验验证与方法对比提供方法基础。

= 实验结果分析

== 实验基本介绍

=== 数据集
<sec4:dataset>

本文使用了三个开源的医学影像分类数据集，数据集概况如@tab-dataset 所示。

#capsubfig(
  (
    (content: image("figures/DermaMNIST.png", width: 100%), subcaption: [DermaMNIST]),
    (content: image("figures/BloodMNIST.png", width: 100%), subcaption: [BloodMNIST]),
    (content: image("figures/aptos.png", width: 100%), subcaption: [APTOS-2019]),
  ),
  columns: 3,
  caption: [使用的数据集概览@medmnistv2],
  label: <fig-dataset>,
)

DermaMNIST@medmnistv2 包含七类皮肤病灶图像，BloodMNIST@medmnistv2 包含八类血细胞图像，两个数据集均来自 MedMNIST 系列医学影像分类数据集，APTOS-2019 包含五类糖尿病视网膜病变等级图像，三个数据集均提供了训练、验证和测试集划分。后续实验中评估指标为测试集上的分类准确率（Accuracy）。

#captab(
  caption: [实验数据集概况],
  label: <tab-dataset>,
)[
  | 数据集        | 图像模态   | 类别数 | 图像尺寸 | 训练/验证/测试 |
  | ------------- | ---------- | ------ | -------- | -------------- |
  | DermaMNIST    | 皮肤镜图像 | 7      | 224×224  | 7,007/1,003/2,005 |
  | BloodMNIST    | 血细胞显微图像 | 8   | 224×224  | 11,959/1,712/3,421 |
  | APTOS-2019    | 视网膜眼底图像 | 5   | 474×358 ~ 4288×2848不等  | 2,930/36/366 |
]

三个数据集的类别分布如@distribution 所示，其中 DermaMNIST 是最严重的不平衡数据集，APTOS-2019 较不平衡。

#capfig(
  image("figures/distribution.png", width: 100%),
  caption: "数据集类别分布",
  label: <distribution>,
)

=== 实现细节

本文所有实验均在单块 NVIDIA GeForce RTX 5090 上进行。扩散模型采用 Stable Diffusion v1.5 作为基础生成模型，VAE 编码器使用 stabilityai/sd-vae-ft-mse。LoRA 微调的秩设为 16，学习率采用余弦调度。全局聚类阶段采用 MiniBatchKMeans 算法，每次仅在小批量样本上更新聚类中心。蒸馏图像生成部分，去噪步数为 200 步，噪声扰动强度为 0.2，无分类器引导强度为 3.0。教师和学生模型采用 ResNet-18 网络，并加载预训练参数。软标签温度设置为 20。教师和学生模型均训练 20 个周期，使用 AdamW 优化器和余弦学习率优化。

== 各 IPC 下的分类准确率

@tab-main 展示了本文方法在三个数据集上不同 IPC 设定下的分类准确率，以及作为参考上界的全量训练准确率（应该达到的理论最大值）。

#let cell(body) = table.cell(inset: (y: 6pt, x: 16pt), body)
// 合并单元格辅助函数
#let colspan(n, body) = table.cell(colspan: n, inset: (y: 6pt, x: 16pt), body)
#let rowspan(n, body) = table.cell(rowspan: n, inset: (y: 6pt, x: 16pt), body)

#figure(
  table(
    columns: 6,
    stroke: none,
    align: (center,) * 6,

    // 顶线
    table.hline(y: 0, stroke: 1.5pt),
    // 表头线
    table.hline(y: 2, stroke: 0.75pt),
    // 底线
    table.hline(y: 5, stroke: 1.5pt),
    // Other
    table.hline(y: 1, stroke: 0.75pt),

    // 第1行（表头）
    table.cell(rowspan: 2, inset: (y: 15pt, x: 16pt), "数据集"),
    colspan(4, [IPC (Image Number Per Class)]),
    table.cell(rowspan: 2, inset: (y: 15pt, x: 16pt), "全量"),

    // 第2行
    cell([10]), cell([50]), cell([100]), cell([200]),

    // 第3行 DermaMNIST
    cell([DermaMNIST]), cell([0.6922]), cell([0.7840]), cell([0.8094]), cell([0.8164]), cell([0.8842]),

    // 第4行 BloodMNIST
    cell([BloodMNIST]), cell([0.9199]), cell([0.9736]), cell([0.9801]), cell([0.9827]), cell([0.9900]),

    // 第5行 APTOS-2019
    cell([APTOS-2019]), cell([0.7508]), cell([0.7815]), cell([0.7952]), cell([0.8054]), cell([0.8395]),
  ),
  caption: "不同 IPC 下的分类准确率",
)<tab-main>


从结果中可看出，随着 IPC 的提高，本文方法在三个数据集上的性能均有一定提升，且在 IPC=200 时接近全量数据训练的性能。DermaMNIST 与全量训练的性能 88.42% 相差约 6 %，相比起 BloodMNIST 和 APTOS-2019 数据集，其性能较差，这可能是由于 DermaMNIST 数据集类别分布极端不均衡，如@sec4:dataset 所述。此外 APTOS-2019 的整体提升幅度较平缓，从 IPC=100 到 IPC=200 性能提升不大。

== 消融分析

为了验证本文提出的部分模块的贡献，本节在 DermaMNIST 数据集上设置了三组消融实验，结果如@tab-ablation 所示，所有实验都使用了微调后的扩散模型。

#captab(
  caption: [DermaMNIST 数据集上的消融实验结果（准确率）],
  label: <tab-ablation>,
)[
  | 配置 | 全局聚类 | 软标签 | IPC=10 | IPC=50 | IPC=100 | IPC=200 |
  | ---- | :-----: | :---: | -----: | -----: | ------: | ------: |
  | A    |         |       |  0.5551 |  0.5875 |  0.7256  |  0.7192  |
  | B    |    ✓    |       |  0.6558 |  0.7052 |  0.7461  |  0.7645  |
  | C    |    ✓    |   ✓   |  *0.6922* |  *0.7840* |  *0.8094*  |  *0.8164*  |
]

配置 A 采用按类别聚类并使用硬标签训练。该配置在各 IPC 下准确率最低，IPC=10 时仅为 55.51%，IPC=200 也只达到 71.92%，相比全量训练的性能差距有 15% 左右。

我认为，逐类独立聚类时，相当于我们添加了一个人为的先验知识，即不同类别的样本在聚类时应该彼此隔离，然而许多医学影像数据都有着类间相似，类内差异大的特性，其不同类别在隐空间中可能处于相近的位置，按类聚类完全忽略了不同类别潜在特征之间的相似性和连续性，导致处于类别重叠区域的信息被人为切断，而对于分类问题，处于决策边界上的样本很大程度上会影响分类模型的性能，这可能就是按类别聚类方法性能不高的原因。此外，人为标注的样本有时可能会出错，如果我们按照类别聚类，相当于把错误标签融入了模型中，这些标注噪声都会影响我们的模型性能。

配置 B 仅将按类别独立聚类替换为全局聚类，就在各 IPC 下获得了大幅的性能提升。这说明使用全局聚类将所有训练样本的潜在特征作为一个整体进行划分，允许不同类别的相似特征被聚合到同一聚类中心，可以捕捉到按类别独立聚类所忽略到的一些特征。

配置 C 进一步加入了软标签训练，即本文的完整方法。软标签的引入在所有 IPC 进一步提升了性能，尤其是中低 IPC 下的提升更加显著。我认为，这是因为数据极小时，硬标签无法为每张合成图像提供足够的监督信息，而教师模型提供的软标签所携带暗知识恰好弥补了这一缺陷，使学生模型精确的学习到决策边界。

@fig-tsne-derma 给出了 DermaMNIST 数据集原始图像在经过了编码器处理后的潜在空间 t-SNE 可视化结果，不同颜色代表不同类别。

#capfig(
  image("figures/dermamnist_latent_tsne.png", width: 80%),
  caption: [DermaMNIST 数据集在隐空间的 t-SNE 可视化],
  label: <fig-tsne-derma>,
)

#capsubfig(
  (
    (content: image("figures/bloodmnist_latent_tsne.png", width: 100%), subcaption: [BloodMNIST 数据集]),
    (content: image("figures/aptos_latent_tsne.png", width: 100%), subcaption: [APTOS-2019 数据集]),
  ),
  columns: 3,
  caption: [其余两个数据集的 t-SNE 可视化],
  label: <fig-tsne-others>,
)

从图中可以观察到，数据点在潜在空间中呈现出显著的交叉混合分布，各类别的样本并非形成独立、清晰的边界，而是相互耦合在一起。在 DermaMNIST 和 BloodMNIST 中，不同类别的潜在特征存在大量重叠区域；APTOS-2019 的样本虽然较为集中，但仍然有少数样本混入其他类别样本附近。

这种跨类别特征交织的分布可以解释全局聚类策略为什么优于按类别聚类。若对每个类别独立执行 K‑Means 聚类，将人为切断相似类别之间具有关联的特征，此外，这些相似的特征（如不同疾病但具有相似组织染色的病理图像区域）在按类别独立聚类时，可能会被强行划分为互不关联且相距较远的聚类中心，导致了信息的损失。

对于如何提升蒸馏数据集在下游任务上的性能，我认为一个朴素且直接的想法是，第一要提升生成样本的多样性，即最大程度地覆盖原始数据集。第二要提升生成样本的真实性。在本文方法中，前者由全局聚类策略来完成，后者则依赖于扩散模型的强大先验来完成。

总结来说，全局聚类不再按照类别独立聚类，最大化地保留了类别模糊地带的信息，并且由于没有类别标签的硬性划分，使得聚类出的簇可以更多的覆盖原始数据集。软标签则充分利用了每张蒸馏图像中的特征和教师模型的知识，提供了更精确的监督信号，两者协同，使得下游模型的训练取得了不错的性能。

== 对比分析

为验证本文方法的有效性，下面将本文方法和一些已有的数据集蒸馏的方法进行对比，主要可以分为两类，一类是直接随机选择和 KMeans 聚类挑选，这两种平凡的方法可以作为基线来验证本文精心设计的数据集蒸馏方法的有效性。第二类是我选择的机制同样基于扩散模型的数据集蒸馏的方法，验证同样利用扩散模型的情况下，本文方法能否优于他们的方法。

=== 与核心集选择方法对比

@tab-compare-full 展示了三种方法在不同 IPC 配置、不同数据集下的分类准确率，其中 Random 表示随机挑选方法，KMeans 表示在隐空间聚类并挑选距离聚类中心最近的样本作为蒸馏数据集，Ours 表示本文的方法。

#let cell(body) = table.cell(inset: (y: 6pt, x: 16pt), body)
// 合并单元格辅助函数
#let colspan(n, body) = table.cell(colspan: n, inset: (y: 6pt, x: 6pt), body)
#let rowspan(n, body) = table.cell(rowspan: n, inset: (y: 6pt, x: 6pt), body)

#figure(
  table(
    columns: 7,
    stroke: none,
    align: center,

    // 顶线
    table.hline(y: 0, stroke: 1.5pt),
    // 表头线
    table.hline(y: 2, stroke: 0.75pt),
    // 分隔线
    table.hline(y: 5, stroke: 0.75pt),
    table.hline(y: 8, stroke: 0.75pt),
    // 底线
    table.hline(y: 11, stroke: 1.5pt),
    table.hline(y: 1, stroke: 0.75pt),

    // === 表头 ===
    // 第1行
    table.cell(rowspan: 2, inset: (y: 16pt, x: 6pt), "数据集"),
    table.cell(rowspan: 2, inset: (y: 16pt, x: 6pt), "方法"),
    colspan(4)[IPC],
    table.cell(rowspan: 2, inset: (y: 16pt, x: 6pt), "全量"),

    // 第2行
    cell[10], cell[50], cell[100], cell[200],

    // === DermaMNIST ===
    // 第3行
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "DermaMNIST"),
    cell[Random],
    cell[0.6040], cell[0.6908], cell[0.7302], cell[0.7461],
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "0.8842"),

    // 第4行
    cell[KMeans],
    cell[0.5531], cell[0.6429], cell[0.6928], cell[0.7002],

    // 第5行
    cell[Ours],
    cell[*0.6922*], cell[*0.7840*], cell[*0.8094*], cell[*0.8164*],

    // === BloodMNIST ===
    // 第6行
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "BloodMNIST"),
    cell[Random],
    cell[0.8632], cell[0.9626], cell[0.9693], cell[0.9716],
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "0.9900"),

    // 第7行
    cell[KMeans],
    cell[0.8454], cell[0.9488], cell[0.9693], cell[0.9813],

    // 第8行
    cell[Ours],
    cell[*0.9199*], cell[*0.9736*], cell[*0.9801*], cell[*0.9827*],

    // === APTOS-2019 ===
    // 第9行
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "APTOS-2019"),
    cell[Random],
    cell[0.6348], cell[0.6928], cell[0.7611], cell[0.7991],
    table.cell(rowspan: 3, inset: (y: 26pt, x: 6pt), "0.8395"),

    // 第10行
    cell[KMeans],
    cell[0.6143], cell[0.7474], cell[0.7577], cell[0.7611],

    // 第11行
    cell[Ours],
    cell[*0.7508*], cell[*0.7815*], cell[*0.7952*], cell[*0.8054*],
  ),
  caption: [不同方法在多个数据集上的性能对比],
) <tab-compare-full>

可以看出本文方法对比其他两种平凡的核心集挑选方法，均取得了更好的成绩，从数据上看，低 IPC 情况下本文方法优势明显，随着 IPC 逐渐升高，与其他两种方法的差距逐渐变小。

=== 与生成式蒸馏方法对比
// TODO 加入引用，在先前节介绍过
@tab-sota 展示了本文方法与其他几种同样基于扩散模型的方法的对比，可以看出对比其他三种方法，本文方法在所有 IPC 舍得下均取得了更高的准确率。

#captab(
  caption: [DermaMNIST 上与主流蒸馏方法的准确率对比（%）],
  label: <tab-sota>,
)[
  | 方法   | IPC=10 | IPC=50 | IPC=100 | IPC=200 |
  | D⁴M    | 0.5551 | 0.5875 | 0.7256  | 0.7251  |
  | MGD³   | 0.5925 | 0.7605 | 0.7960  | 0.8139  |
  | DDQO   | 0.6169 | 0.7600 | 0.7980  | 0.8119  |
  | Ours   | *0.6922* | *0.7840* | *0.8094*  | *0.8164* |
]

D⁴M 是最简单的扩散模型蒸馏的方法，可以看到 IPC 从 100 到 200 情况下性能提升并不明显，说明按类别聚类难以覆盖数据的主要模式，增加聚类中心数量带来的边际收益已经十分有限。MGD³ 和 DDQO 的性能曲线非常接近，但均未能超越 Ours。

@fig-derma-acc-compare 和@fig-derma-loss-compare 还展示了在 DermaMNIST 数据集上，IPC 设置 200 的情况下本文方法与其他方法的测试 loss 和测试准确率的对比。可以看出本文方法训练时收敛更快，最终性能更佳。

#capfig(
  image("figures/dermamnist_ipc200_test_acc.png", width: 100%),
  caption: [DermaMNIST 数据集上本文方法与其他方法测试集准确率对比（IPC=200）],
  label: <fig-derma-acc-compare>,
)

#capfig(
  image("figures/dermamnist_ipc200_test_loss.png", width: 100%),
  caption: [DermaMNIST 数据集上本文方法与其他方法测试集损失对比（IPC=200）],
  label: <fig-derma-loss-compare>,
)

== 定性分析

=== 蒸馏数据集概览

为直观展示本文方法生成的蒸馏图像质量，@fig-distilled-preview 给出了在 APTOS-2019、DermaMNIST 和 BloodMNIST 三个数据集上 IPC=200 设定下生成的蒸馏图像的部分预览。从视觉上可以看出，基于扩散模型生成的蒸馏图像很好地保留了各类别病灶的形态、纹理和色调等判别性视觉特征，且在不同聚类中心对应的生成图像之间呈现出合理的类内多样性。与基于匹配范式的蒸馏方法通常产生的模糊、缺乏细节的合成图像相比，本文方法借助扩散模型的强大先验，使蒸馏图像在真实性方面有明显提升。

#capsubfig(
  (
    (content: image("figures/distilled_preview_dermamnist_ipc100.jpg", width: 100%), subcaption: [DermaMNIST]),
    (content: image("figures/distilled_preview_bloodmnist_ipc100.jpg", width: 100%), subcaption: [BloodMNIST]),
    (content: image("figures/distilled_preview_aptos_ipc100.jpg", width: 100%), subcaption: [APTOS-2019]),
  ),
  columns: 3,
  caption: [三个数据集上的蒸馏图像预览（IPC=200）],
  label: <fig-distilled-preview>,
)

@fig-prev-dd-visual 展示了先前数据集蒸馏方法（如分布匹配、梯度匹配等）生成的典型蒸馏图像。可以看出，这些方法由于直接在像素或特征空间中进行数值优化，蒸馏图像往往呈现噪声状、模糊或块状伪影，视觉语义信息严重退化，本质上是在性能指标与视觉可辨识程度之间做了取舍。相比之下，本文基于扩散模型的方法使蒸馏图像在保留类别判别性的同时展现出与真实医学图像一致的纹理和解剖特征。

#capfig(
  image("figures/visualization_previous_DD_methods.png", width: 100%),
  caption: [基于匹配的方法生成的蒸馏图像，丧失了视觉语义特征],
  label: <fig-prev-dd-visual>,
)

=== 训练时间对比

本节对比了全量数据训练与蒸馏数据训练在 DermaMNIST 数据集上的总训练时间结果如@tab-time 所示，这里测试训练时间的实验运行在 RTX 4070 ti super 显卡上。

#figure(
  table(
    columns: 4,
    stroke: none,
    align: (center,) * 3,

    // 顶线
    table.hline(y: 0, stroke: 1.5pt),
    // 表头线
    table.hline(y: 2, stroke: 0.75pt),
    // 底线
    table.hline(y: 6, stroke: 1.5pt),
    table.hline(y: 1, stroke: 0.75pt),

    // 第1行（表头）
    table.cell(rowspan: 2, inset: (y: 15pt, x: 6pt), "IPC"),
    table.cell(colspan: 2, inset: (y: 6pt, x: 6pt), "训练时间 (秒)"),
    table.cell(rowspan: 2, inset: (y: 15pt, x: 6pt), "速度提升 (倍)"),

    // 1+1 行
    cell("全量训练"), cell("蒸馏训练"),

    // 第2行
    cell("10"), table.cell(rowspan: 4, inset: (y: 34pt, x: 6pt), [118.1]), cell([33.5]), cell([3.5]),

    // 第3行
    cell([50]), cell([38.3]), cell([3.1]),

    // 第4行
    cell([100]), cell([43.9]), cell([2.7]),

    // 第5行
    cell([200]), cell([52.9]), cell([2.2]),
  ),
  caption: "DermaMNIST 数据集上训练时间比较",
)<tab-time>

从@tab-time 可以看出，得益于样本数量的减少，蒸馏训练不出所料地缩短了训练时间。以 DermaMNIST 为例，在 IPC 为 100 的情况下，全量训练需处理 7007 张图像，而蒸馏训练仅使用 700 张（减少 90.0%）。此外，由于蒸馏数据集规模极小，使得模型可以在更少的迭代步数内完成收敛。上述结果表明，本文方法在保持较高分类精度的同时，大幅降低了训练计算成本，验证了数据集蒸馏在医学图像高效训练场景中的实用价值。

== 本章小结

本章在三个医学影像分类数据集上评估了基于扩散模型与全局聚类的数据集蒸馏方法。不同 IPC 设定下的分类实验结果表明，该方法在所有配置下均获得了有竞争力的准确率，IPC=200 时的性能已接近全量数据训练水平。

消融实验展示了全局聚类与软标签训练各自的贡献。得益于全局聚类保留了位于类别重叠区域的信息，各 IPC 下的准确率均有大幅提升。软标签训练进一步带来增益，中低 IPC 条件下提升较为显著，教师模型提供的暗知识弥补了样本极度匮乏时监督信息的不足。此外，对潜在空间进行了 t-SNE 可视化，显示医学图像在隐空间中呈跨类别交叉混合分布，一定程度上解释了为什么全局聚类相较于按类别独立聚类更加有效。

与核心集选择方法及现有生成式蒸馏方法的对比中，本文方法在所有数据集和 IPC 配置下均取得了最优结果。低 IPC 条件下，相对于随机选择和 K‑Means 聚类的优势尤为突出；与 D⁴M、MGD³ 和 DDQO 等基于扩散模型的方法相比，该方法在收敛速度和最终性能上均表现出了一致优势。

定性分析显示，生成的蒸馏图像较好地保留了病灶的形态、纹理和色调，视觉真实性明显优于传统匹配范式产生的模糊或噪声状合成图像。训练时间对比表明，蒸馏训练在压缩数据规模的同时大幅降低了计算成本，体现出数据集蒸馏在医学图像高效训练中的实用价值。

整体来看，全局聚类策略与软标签知识迁移的协同设计充分发挥了扩散模型先验的潜力，在维持较高分类精度的同时显著提升了训练效率。

= 总结与展望

== 工作总结

本文的目标是用极少的样本实现高效的医学影像分类。围绕这个目标，提出了一种数据集蒸馏方法。该方法把扩散模型和LoRA微调技术结合起来，也用了潜在空间里的全局聚类方法。它能把大规模的医学图像训练集压缩成一个信息很集中的合成数据集。这样一来，存储数据和训练模型的开销都大大降低了，同时分类性能也接近用全部数据训练出来的水平。全文工作总结如下。

本文梳理了数据集蒸馏领域的发展脉络和关键的技术基础。先从双层优化的定义讲起，回顾了梯度匹配、轨迹匹配和分布匹配这三种主流方法的核心思路以及各自适合的范围。针对大规模数据的情况，重点分析了像SRe2L这种解耦式方法，看它是怎么通过“压缩‑恢复‑重标记”这三个步骤来突破规模限制的。然后聚焦于基于扩散模型的生成式蒸馏方法，仔细分析了D⁴M、MGD³和DDQO这几个代表性工作的设计思路，并指出它们在处理医学图像时共同存在的三点不足。第一点是缺少对领域特点的适配，导致通用的扩散模型很难准确学习医学图像里特有的纹理和结构。第二点是逐类独立聚类忽略了不同类别之间共享的特征，造成判别信息的损失。第三点是在软标签训练过程中，实时的数据增强和教师模型的前向传播带来了很多不必要的计算。这些分析为本文方法的设计指明了方向。

针对以上不足，本文提出了三项改进。

第一项是专门为医学图像设计的扩散模型轻量级领域适配方案。在Stable Diffusion v1.5的基础上，在U‑Net的交叉注意力层里加入LoRA低秩分解矩阵，只训练不到原模型千分之一的参数。这样，扩散模型在保留大规模自然图像先验的同时，也能很快适应医学图像特有的灰度分布、纹理模式和解剖结构，为后面的潜在空间聚类和蒸馏图像合成打下基础。

第二项是潜在空间里的全局聚类加多数投票机制。不再用现有方法里那种逐类独立聚类的做法，而是把所有训练样本先通过冻结的VAE编码器映射到潜在空间，然后一起输入到MiniBatchKMeans里做全局聚类。聚类完成后，根据每个簇里样本标签的多数投票结果，给每个聚类中心定一个类别。这种策略不需要提前为每个类别分配固定数量的聚类中心，而是让聚类中心自然地适应数据的疏密分布。样本密集的地方会得到更多聚类中心，从而充分覆盖类内的多样性；样本稀疏的少数类区域则用较少的聚类中心，避免被噪声样本过度干扰。更重要的是，全局聚类允许不同类别但在特征空间中位置相近的样本被分到同一个聚类中心，从而捕捉到逐类聚类无法覆盖的跨类别特征共享关系，以及类别边界附近的判别信息。

第三项是基于软标签的教师‑学生训练框架。用一个在原始数据集上训练好的ResNet‑18教师模型，为蒸馏图像生成经过温度平滑的概率分布，作为软标签。和独热硬标签相比，软标签携带了丰富的类别间相似性结构，在样本极少的情况下能为学生模型提供更精细的监督信号，让学生模型能从蒸馏图像中学到判别性的知识。

我们在三个开源的医学数据集上做了全面的实验验证。这三个数据集分别是DermaMNIST、BloodMNIST和APTOS‑2019，涵盖了皮肤镜图像、血细胞显微图像和视网膜眼底图像。在不同IPC设定下的分类结果显示，当IPC等于200时，本文方法在这三个数据集上的准确率分别达到了81.64%、98.27%和80.54%，与全量训练的性能差距分别被缩小到了大约6.7、0.7和3.4个百分点。消融实验定量分析了各个模块的贡献。和逐类独立聚类相比，全局聚类策略在不同IPC下带来了大约4到10个百分点的准确率提升，这说明跨类别信息的保留在医学图像蒸馏中非常关键。再引入软标签训练后，在中低IPC条件下又额外获得了大约3到8个百分点的提升。我们把本文方法与随机选择、K‑Means近邻选取这两类核心集基线，以及D⁴M、MGD³、DDQO这三种基于扩散模型的蒸馏方法做了系统对比，结果显示本文方法在所有数据集和IPC配置下都取得了最好的结果，而且训练收敛更快，最终性能也更高。潜在空间的t‑SNE可视化结果表明，医学图像的特征在隐空间中呈现出跨类别交叉混合的分布，这从几何直观上解释了为什么全局聚类比逐类独立聚类更好。此外，定性分析显示，蒸馏图像在病灶形态、纹理和色调等判别性特征上保持了较好的真实感和类内多样性。训练时间的对比表明，在图像数量减少大约90%的情况下，蒸馏训练明显缩短了训练时间，体现了本文方法在数据和计算两方面压缩下的实用价值。

综上所述，本文以扩散模型的生成先验为基础，用全局聚类策略尽可能多地覆盖数据分布中的判别信息，再结合软标签知识迁移，高效地完成了医学影像分类任务，为资源受限场景下的医学影像分类提供了一条兼顾精度和效率的可行思路。
== 未来展望

尽管本文方法在多个医学图像数据集上取得了良好效果，但在理论深度、方法鲁棒性与应用广度等方面仍存在不足。未来可从以下几个方向进一步探索。

聚类算法的理论保障与稳定性提升。本文采用MiniBatchKMeans进行全局聚类，虽在实践中表现良好，但该方法受初始质心选择和批量采样顺序影响较大。多次运行可能产生不同的聚类中心分布，导致下游性能波动。未来可探索通过确定性质心初始化方案（如K‑Means++初始化）或引入聚类集成策略来提升可复现性。更长远地，可尝试从信息论的率失真理论或最优传输角度，为全局聚类策略提供理论分析框架，定量刻画聚类中心数量、压缩比与分类性能损失之间的内在关系，为IPC的选择和聚类中心数量的配置提供理论指导。

蒸馏图像生成的端到端可微优化。当前蒸馏图像生成与下游学生训练是两阶段独立流程，蒸馏阶段没有利用训练反馈信号进行自适应调整，导致合成图像在某种意义上不是最优的。如果能在聚类中心引导去噪过程中引入可微的反馈通路，例如将学生模型的分类损失沿去噪轨迹反向传播来微调聚类中心或生成参数，将有望实现面向下游任务的端到端蒸馏优化，进一步提升蒸馏数据的信息密度和训练有效性。

大规模高分辨率医学影像的可扩展蒸馏。本文实验主要在MedMNIST系列中的中小尺寸数据集上开展。当面向全切片病理图像、高分辨率CT体积或三维MRI等实际临床场景时，VAE潜在特征维度更高，数据量更大，全局聚类的计算与内存开销将显著增长。未来可探索分块聚类、层次化聚类中心聚合或基于近似最近邻索引的分布式聚类策略，在保持全局聚类优势的同时提升可扩展性。

面向非独立同分布与联邦学习场景的适配。在真实医疗环境中，数据常以非独立同分布的形式分散在不同机构之间，直接集中训练不可行。本文方法的蒸馏框架天然具备数据压缩与隐私保护的双重属性。未来可探索将全局聚类拓展为联邦场景下的分布式聚类协议，在各参与方本地完成编码与局部统计量的计算后，汇聚至中心端进行聚类中心聚合，实现跨机构的隐私保护式协作蒸馏。

多任务与多模态蒸馏的扩展。本文方法目前只面向单标签图像分类任务。未来可尝试将蒸馏框架拓展至图像分割、目标检测等密集预测任务，探索潜在空间中适用于像素级标注的聚类中心生成与去噪重建机制。此外，医学数据常以影像、文本报告、基因组学数据等多种模态共存。将这些模态的信息统一编码到共享潜在空间中进行联合蒸馏，有望生成多模态对齐的合成样本，服务于更复杂的临床辅助诊断场景。
